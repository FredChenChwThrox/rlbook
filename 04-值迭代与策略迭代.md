# 第4章：值迭代与策略迭代（Value Iteration and Policy Iteration）

> 本章讨论已知环境模型时的经典动态规划方法。它们的共同前提是：转移概率和奖励结构已知。

---

## 4.1 值迭代（Value Iteration）

### 算法动机

第3章给出了贝尔曼最优方程，但那只是“最优值满足什么条件”，还不是实际算法。

值迭代的思路是：

> 反复应用贝尔曼最优更新，直到值函数稳定。

### 核心思想

使用贝尔曼最优算子：

$$(T^*v)(s)=\max_a \sum_{s'} p(s'|s,a)\left[r+\gamma v(s')\right]$$

白话解释：

- 先假设你已经有一版值函数 $v$；
- 再用“选最优动作”的方式把它整体更新一次；
- 不断重复后，值函数会逼近最优值函数。

### 算法步骤（伪代码）

```text
初始化所有状态值 v(s)
重复：
    对每个状态 s：
        v(s) ← max_a Σ_{s'} p(s'|s,a)[r + γ v(s')]
直到变化足够小

再根据最终 v(s) 提取贪婪策略
```

### 数学公式详解

值迭代更新式：

$$v_{k+1}(s)=\max_a \sum_{s'} p(s'|s,a)\left[r+\gamma v_k(s')\right]$$

公式解释：

- $v_k$ 是第 $k$ 轮估计；
- 右边在计算“如果此刻做最优动作，当前一步奖励加未来估计值是多少”；
- 更新后的 $v_{k+1}$ 比旧估计更接近最优值。

### 直观理解

可以把它理解为“不断修订每个状态的最佳前景预测”。

### 收敛性分析

常见结果是：

$$\|v_{k+1}-v^*\|_\infty \le \gamma \|v_k-v^*\|_\infty$$

白话解释：

- 每迭代一次，和真正最优值的最大误差会按 $\gamma$ 的比例缩小。
- 所以 $\gamma$ 越接近 1，收敛通常越慢。

---

## 4.2 策略迭代（Policy Iteration）

### 算法动机

值迭代每一步都要做“对动作取最大值”。策略迭代采用另一种思路：

- 先评估当前策略；
- 再按当前值函数改进策略。

### 核心思想

策略迭代由两部分循环组成：

1. 策略评估：算出当前策略的值函数。
2. 策略改进：对每个状态做贪婪替换。

### 算法步骤（伪代码）

```text
初始化一个策略 π
重复：
    1. 计算当前策略 π 的值函数 vπ
    2. 对每个状态 s：
           π(s) ← argmax_a Σ_{s'} p(s'|s,a)[r + γ vπ(s')]
直到策略不再变化
```

### 数学公式详解

策略评估阶段满足：

$$v_\pi(s)=\sum_{s'} p(s'|s,\pi(s))\left[r+\gamma v_\pi(s')\right]$$

策略改进阶段通常写为：

$$\pi_{\text{new}}(s)\in \arg\max_a \sum_{s'} p(s'|s,a)\left[r+\gamma v_\pi(s')\right]$$

公式解释：

- 第一个公式是在“已知当前策略”时计算它到底值多少；
- 第二个公式是在“固定这些值”的前提下，把每个状态下的动作换成更优的。

### 策略改进定理（Policy Improvement Theorem）

这一定理说明：如果按动作值做贪婪改进，新策略不会比旧策略差。

### 策略评估的简化版本

实际中可以不必每轮都精确求到完全收敛，这就引出截断策略迭代。

---

## 4.3 截断策略迭代（Truncated Policy Iteration）

### 算法动机

完整策略评估可能代价较高，因此可以只做有限次评估更新，再进行改进。

### 核心思想

- 不把当前策略评估到完全精确；
- 只做几轮近似评估；
- 然后立刻做策略改进。

### 算法步骤（伪代码）

```text
初始化策略 π 和状态值 v
重复：
    先对当前策略做若干轮评估更新
    再按当前 v 做一次贪婪改进
直到策略稳定
```

### 三种算法的统一视角

- 值迭代：极端偏向频繁改进。
- 策略迭代：极端偏向充分评估。
- 截断策略迭代：在两者之间折中。

### 广义策略迭代（GPI）的重要性

GPI 的核心思想是：

> 评估和改进可以交替进行，不必等其中一方完全结束。

这个思想会一直延续到后面的 TD、Actor-Critic 等方法。

---

## 三种算法对比总结

| 方法 | 特点 | 适合的理解方式 |
|:---:|:---|:---|
| 值迭代 | 每轮都做最优更新 | 直接逼近最优值 |
| 策略迭代 | 评估与改进分阶段进行 | 先看清当前策略，再整体改良 |
| 截断策略迭代 | 在两者之间折中 | 更接近实际大规模算法思路 |

---

## 本章总结

- 本章把贝尔曼最优方程变成了可执行算法。
- 值迭代重在反复更新值函数。
- 策略迭代重在“评估当前策略，再做改进”。
- GPI 是后续很多 RL 算法的共同思想来源。
