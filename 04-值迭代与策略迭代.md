# 第4章：值迭代与策略迭代（Value Iteration and Policy Iteration）

> **章节定位**：本章介绍三个密切相关的算法，它们都属于**动态规划（Dynamic Programming, DP）**方法家族。这些方法的核心特点是**需要系统模型（model-based）**——即智能体必须知道环境的转移概率 $p(s'|s,a)$ 和奖励函数 $r(s,a)$。这就像下棋时你不仅要知道当前局势，还要清楚每一步棋之后对手所有可能的应对方式。

---

## 4.1 值迭代（Value Iteration）

### 算法动机

在上一章中，我们了解到**贝尔曼最优方程（Bellman Optimality Equation）**是强化学习的核心方程。对于一个有限马尔可夫决策过程（MDP），贝尔曼最优方程的形式为：

$$v^*(s) = \max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v^*(s')\right]$$

> **中文解释**：状态 $s$ 的最优值 $v^*(s)$ 等于：选择能使"即时奖励 $r$ 加上折扣后的下一状态最优值 $\gamma v^*(s')$"的期望最大的那个动作 $a$。

这个方程本质上是一个方程组（每个状态一个方程），如果能直接求解，就能得到最优值函数。但对于大型MDP，直接解析求解不现实。**值迭代（Value Iteration）**算法应运而生——它通过反复迭代来逼近这个方程的解。

### 核心思想

值迭代的核心思想极其简洁优雅：

> **反复应用贝尔曼最优算子（Bellman Optimality Operator）$T^*$，直到状态值收敛到最优值。**

从数学上看，贝尔曼最优算子 $T^*$ 定义为：

$$(T^* v)(s) = \max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v(s')\right]$$

> **中文解释**：算子 $T^*$ 将一个值函数 $v$ "变换"为另一个值函数 $T^*v$。变换后的新值函数在每个状态 $s$ 处的值，等于在 $s$ 处采取最优动作所能获得的期望回报。

**为什么这个方法有效？** 这归功于**收缩映射定理（Contraction Mapping Theorem）**：贝尔曼最优算子 $T^*$ 是一个**$\gamma$-收缩映射**，其中 $\gamma \in [0,1)$ 是折扣因子。这意味着每次应用 $T^*$，值函数之间的距离至少缩小为原来的 $\gamma$ 倍。因此，反复迭代必然收敛到唯一的不动点——即最优值函数 $v^*$。

### 算法步骤（伪代码）

```
算法：值迭代（Value Iteration）
─────────────────────────────────────────
输入：MDP模型 (S, A, P, R, γ)，收敛阈值 θ > 0
输出：最优值函数 v* 和最优策略 π*

1. 初始化：
   对所有状态 s ∈ S：
       v(s) ← 0                          // 零初始化

2. 重复直到收敛：
   Δ ← 0                                // 记录最大变化量
   
   对每个状态 s ∈ S：
       v_old ← v(s)                     // 保存旧值
       
       // 贝尔曼最优更新：取所有动作中的最大值
       v(s) ← max_a Σ_{s'} p(s'|s,a)[r(s,a,s') + γ·v(s')]
       
       Δ ← max(Δ, |v_old - v(s)|)       // 更新最大变化量
   
   直到 Δ < θ                            // 当变化足够小时停止

3. 提取最优策略（贪婪策略）：
   对每个状态 s ∈ S：
       π(s) ← argmax_a Σ_{s'} p(s'|s,a)[r(s,a,s') + γ·v(s')]

4. 返回 v 和 π
─────────────────────────────────────────
```

### 数学公式详解

**迭代更新公式：**

$$v_{k+1}(s) = \max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v_k(s')\right]$$

> **中文解释**：在第 $k+1$ 次迭代中，状态 $s$ 的新值 $v_{k+1}(s)$ 由两部分组成：(1) 即时奖励 $r$；(2) 折扣后的下一状态值 $\gamma v_k(s')$。我们对所有可能的下一状态 $s'$ 按转移概率 $p(s'|s,a)$ 加权求和，得到采取动作 $a$ 的期望值，然后在所有动作中取最大值。

**贪婪策略提取：**

$$\pi(s) = \arg\max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v_k(s')\right]$$

> **中文解释**：当值函数收敛后，最优策略 $\pi$ 在每个状态 $s$ 处选择那个能使"即时奖励 + 折扣未来值"的期望最大的动作。

### 直观理解

> **生活类比：寻宝地图的迭代完善**
> 
> 想象你有一张藏宝地图，上面标注了很多地点。起初，你只知道每个地点离宝藏的大致距离（全部设为0）。然后你开始反复检查每个地点："从当前地点出发，选择哪个方向（动作）能让我最快到达宝藏？"每次检查，你都更新对该地点到宝藏距离的认识。经过足够多的迭代，地图上每个地点标注的距离都趋于准确，最终你规划出了从任意地点到宝藏的最优路线。

**算法流程图（文字版）：**

```
┌──────────────┐
│  初始化 v=0   │
└──────┬───────┘
       ▼
┌──────────────────────┐     否
│ 对每个s应用贝尔曼最优  │────────────┐
│ 算子，得到新值v_{k+1} │            │
└──────┬───────────────┘            │
       ▼                             │
┌──────────────────────┐            │
│  |v_{k+1} - v_k| < θ? │            │
└──────┬───────────────┘            │
       │ 是                          │
       ▼                             │
┌──────────────┐                     │
│ 提取贪婪策略π* │                     │
└──────────────┘                     │
                                     ▼
                              ┌──────────────┐
                              │ 继续下一次迭代 │
                              └──────────────┘
```

### 收敛性分析

值迭代的收敛速度是**线性**的，收敛率由折扣因子 $\gamma$ 决定：

$$\|v_{k+1} - v^*\|_\infty \leq \gamma \|v_k - v^*\|_\infty$$

> **中文解释**：值函数与最优值函数的无穷范数距离（即所有状态中的最大偏差）每次迭代至少缩小为原来的 $\gamma$ 倍。$\gamma$ 越小（如0.5），收敛越快；$\gamma$ 越接近1（如0.99），收敛越慢。

---

## 4.2 策略迭代（Policy Iteration）

### 算法动机

值迭代虽然简洁，但每次迭代都要对所有动作取最大值（涉及 $\max_a$ 操作）。策略迭代（Policy Iteration）采用了不同的思路：**交替进行策略评估和策略改进**，通常能在更少的迭代次数内收敛。

### 核心思想

策略迭代由两个步骤交替组成，如同"评估-改进"的循环：

1. **策略评估（Policy Evaluation）**：给定当前策略 $\pi_k$，计算该策略下的状态值函数 $v_{\pi_k}$
2. **策略改进（Policy Improvement）**：利用 $v_{\pi_k}$ 贪婪地改进策略，得到更好的策略 $\pi_{k+1}$

> **生活类比：厨师改良菜谱**
> 
> 想象一位厨师想要做出最好的招牌菜。他先按照现有菜谱做一遍（**策略评估**——评估当前策略的好坏），然后品尝后找出哪里可以改进："如果多加一点香料会不会更好？"（**策略改进**——贪婪地改进策略）。他不断重复这个过程——做菜、品尝、改良菜谱——直到菜谱无法再改进，那就是最优菜谱了。

### 算法步骤（伪代码）

```
算法：策略迭代（Policy Iteration）
─────────────────────────────────────────
输入：MDP模型 (S, A, P, R, γ)
输出：最优策略 π* 和最优值函数 v*

1. 初始化：
   任意选择一个初始策略 π（如随机策略）

2. 重复以下两个步骤直到策略不再改变：

   ┌────────────────────────────────────────┐
   │ 步骤A：策略评估（Policy Evaluation）    │
   │                                        │
   │ 重复直到 v 收敛：                       │
   │   对每个状态 s：                        │
   │     v(s) ← Σ_{s'} p(s'|s,π(s))[r+γ·v(s')]│
   │                                        │
   │ // 注意：这里只考虑当前策略π指定的动作    │
   │ // 不对所有动作取max                     │
   └────────────────────────────────────────┘
                   │
                   ▼
   ┌────────────────────────────────────────┐
   │ 步骤B：策略改进（Policy Improvement）   │
   │                                        │
   │ policy-stable ← true                   │
   │ 对每个状态 s：                          │
   │   old-action ← π(s)                    │
   │   π(s) ← argmax_a Σ_{s'}p(s'|s,a)[r+γ·v(s')]│
   │   如果 old-action ≠ π(s)：              │
   │      policy-stable ← false             │
   │                                        │
   │ 如果 policy-stable = true：             │
   │    返回 π 和 v   // 策略已收敛          │
   └────────────────────────────────────────┘
─────────────────────────────────────────
```

### 数学公式详解

**策略评估（贝尔曼方程）：**

$$v_{\pi_k}(s) = \sum_{s'} p(s'|s, \pi_k(s))\left[r + \gamma v_{\pi_k}(s')\right]$$

> **中文解释**：策略评估求解的是**贝尔曼期望方程**而非最优方程。因为策略 $\pi_k$ 是固定的，我们不取最大值，而是直接代入策略指定的动作 $\pi_k(s)$。这是一个线性方程组，可以用迭代法求解。

**策略改进：**

$$\pi_{k+1}(s) = \arg\max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v_{\pi_k}(s')\right]$$

> **中文解释**：利用刚刚评估得到的 $v_{\pi_k}$，我们在每个状态贪婪地选择那个能带来最大期望回报的动作，从而得到改进后的新策略 $\pi_{k+1}$。

### 策略改进定理（Policy Improvement Theorem）

策略迭代的收敛性由**策略改进定理**保证：

> 如果策略 $\pi'$ 是通过对 $v_\pi$ 贪婪地改进策略 $\pi$ 得到的，那么 $\pi'$ 一定不差于 $\pi$（即对所有状态 $s$，$v_{\pi'}(s) \geq v_\pi(s)$）。如果存在某个状态使得 $\pi'(s) \neq \pi(s)$，则 $\pi'$ 严格优于 $\pi$。

这意味着每次策略改进都会让策略变好（或至少不变），因此算法必然收敛到最优策略。

### 策略评估的简化版本

策略评估每次都需要迭代到收敛，这可能很慢。实际中常用的简化版本是：

```
// 简化策略评估（只做k步迭代而非完全收敛）
重复 k 次：
  对每个状态 s：
    v(s) ← Σ_{s'} p(s'|s,π(s))[r + γ·v(s')]
```

当 $k=1$ 时，这退化为值迭代；当 $k=\infty$ 时，这是完整的策略迭代。

---

## 4.3 截断策略迭代（Truncated Policy Iteration）

### 算法动机

值迭代和策略迭代看起来是两个截然不同的算法，但它们实际上共享一个统一的结构。截断策略迭代（Truncated Policy Iteration）揭示了这种统一性，并在这两个极端之间提供了更灵活的折中方案。

### 核心思想

| 算法 | 策略评估步数 | 特点 |
|------|------------|------|
| **值迭代** | 1步（不做策略评估，直接贪婪更新） | 每次迭代快，但可能需要更多迭代次数 |
| **策略迭代** | 到完全收敛（∞步） | 每次迭代慢，但通常迭代次数少 |
| **截断策略迭代** | k步（有限步数） | 灵活折中，实践中效果往往最好 |

截断策略迭代只做**有限步（k步）**的策略评估就进行策略改进，而不是等到完全收敛。

### 算法步骤（伪代码）

```
算法：截断策略迭代（Truncated Policy Iteration）
─────────────────────────────────────────
输入：MDP模型 (S, A, P, R, γ)，截断参数 k ≥ 1
输出：最优策略 π* 和最优值函数 v*

1. 初始化：
   对所有状态 s：v(s) ← 0
   任意初始策略 π

2. 重复直到策略不再改变：
   
   // 步骤A：截断策略评估（只做k步）
   对 j = 1, 2, ..., k：
     对每个状态 s：
       v(s) ← Σ_{s'} p(s'|s,π(s))[r + γ·v(s')]
   
   // 步骤B：策略改进
   policy-stable ← true
   对每个状态 s：
     old-action ← π(s)
     π(s) ← argmax_a Σ_{s'}p(s'|s,a)[r+γ·v(s')]
     如果 old-action ≠ π(s)：
       policy-stable ← false
   
   如果 policy-stable = true：返回 π 和 v
─────────────────────────────────────────
```

### 三种算法的统一视角

三种算法可以统一在一个框架下理解——**广义策略迭代（Generalized Policy Iteration, GPI）**：

```
┌──────────────────────────────────────────┐
│         广义策略迭代（GPI）框架            │
│                                          │
│   ┌──────────┐      值函数v       ┌──────┐ │
│   │ 策略评估  │◄─────────────────│ 策略  │ │
│   │ （任意步数）│                  │ 改进 │ │
│   └─────┬────┘                  └──┬───┘ │
│         │                          │      │
│         └───────── 策略π ──────────┘      │
│                                          │
│   值迭代：评估1步 → 改进 → 评估1步 → ... │
│   策略迭代：评估∞步 → 改进 → 评估∞步 → ...│
│   截断迭代：评估k步 → 改进 → 评估k步 → ...│
└──────────────────────────────────────────┘
```

> **生活类比：学生复习与考试**
> 
> - **值迭代**像一个学生每学完一章就立刻跳到下一章（评估只做1步就改进），学得很快但理解可能不够深入，需要多轮复习。
> - **策略迭代**像一个学生必须把每一章都完全吃透才继续（评估到完全收敛），每轮学习很慢但进步扎实，轮次少。
> - **截断策略迭代**则是介于两者之间——每章认真学几遍（评估k步）就继续前进，是最实用的学习方法。

### 广义策略迭代（GPI）的重要性

GPI的思想不仅在动态规划中重要，它贯穿于整个强化学习：

- **值迭代**中：值函数更新后立刻贪婪地更新策略
- **策略迭代**中：值函数收敛后再更新策略
- **Q-learning**中：Q值更新后隐式地更新了策略
- **Actor-Critic**中：Critic评估值函数，Actor改进策略

这种"值与策略交互更新"的双轮驱动思想，是强化学习的核心范式之一。

---

## 三种算法对比总结

| 特性 | 值迭代 | 策略迭代 | 截断策略迭代 |
|------|--------|---------|------------|
| 策略评估步数 | 1步 | ∞步（到收敛） | k步（有限步） |
| 每轮迭代速度 | 快 | 慢 | 中等 |
| 收敛迭代次数 | 较多 | 较少 | 中等 |
| 实现复杂度 | 简单 | 中等 | 中等 |
| 是否需要维护策略 | 不需要 | 需要 | 需要 |
| 适用场景 | 简单MDP | 需要精确策略的场景 | 大规模问题（推荐） |

## 本章总结

1. **值迭代**直接迭代贝尔曼最优方程，通过反复应用收缩的贝尔曼最优算子收敛到最优值函数。

2. **策略迭代**交替进行策略评估（求解贝尔曼期望方程）和策略改进（贪婪更新），由策略改进定理保证收敛。

3. **截断策略迭代**统一了前两种算法，通过控制策略评估的步数在效率和收敛速度之间取得平衡。

4. 三种算法都属于**广义策略迭代（GPI）**框架，体现了"值函数与策略交互更新"的核心思想。这一思想将贯穿后续所有强化学习算法。

5. 所有动态规划方法都假设**已知系统模型**（$p(s'|s,a)$ 和 $r$），这是它们的主要局限性。下一章我们将进入**无模型（model-free）**方法，从实际交互数据中学习最优策略。

---

> **延伸阅读提示**：想要更深入理解动态规划方法的读者，建议阅读 Sutton & Barto《Reinforcement Learning: An Introduction》第4章。书中对策略改进定理有严格的数学证明，并提供了更多数值示例。
