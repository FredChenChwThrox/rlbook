# 第5章：蒙特卡洛方法（Monte Carlo Methods）

## 5.1 动机示例：均值估计

### 问题设定

蒙特卡洛方法的基本想法是：

> 不去解析求期望，而是靠样本平均来估计。

### 方法1：批量均值估计（Batch Mean Estimation）

如果已经拿到很多样本 $x_1,\dots,x_n$，均值可写成：

$$\bar{x}_n = \frac{1}{n}\sum_{i=1}^n x_i$$

### 方法2：增量均值估计（Incremental Mean Estimation）

增量写法是：

$$\bar{x}_{n+1} = \bar{x}_n + \frac{1}{n+1}(x_{n+1}-\bar{x}_n)$$

白话解释：

- 新均值 = 旧均值 + 一点修正；
- 修正大小取决于“新样本和旧均值差多少”。

这个结构和后面很多 RL 更新公式非常像。

---

## 5.2 MC Basic算法

### 从动态规划到蒙特卡洛

动态规划依赖环境模型，蒙特卡洛不需要模型。它直接从完整回合经验中估计值函数。

### 算法步骤（伪代码）

```text
重复生成完整回合
对回合中访问到的每个状态：
    计算该状态之后的实际回报 G
    用样本平均更新 V(s)
```

核心更新可写成：

$$V(s) \leftarrow V(s) + \alpha \big(G - V(s)\big)$$

白话解释：

- 如果这次真实回报 $G$ 比当前估计高，就往上调；
- 如果更低，就往下调。

### 从状态值到动作值

控制问题里更常估计动作值 $Q(s,a)$，因为它能直接用于改进策略。

---

## 5.3 探索与利用（Exploration vs Exploitation）

### 核心矛盾

强化学习总在两个目标之间拉扯：

- 利用：优先选择当前看起来最好的动作；
- 探索：尝试不确定的动作，以免错过更好方案。

### ε-贪婪策略（ε-Greedy Policy）

常见做法是：

- 以 $1-\varepsilon$ 的概率选当前最优动作；
- 以 $\varepsilon$ 的概率随机探索。

学习提示：

- 没有探索，可能陷入局部最优；
- 探索太多，又会影响学习效率。

---

## 5.4 MC Exploring Starts算法

### 问题背景

如果有些状态-动作对从不被访问，它们的价值就无法被学习。

### 解决方案：Exploring Starts

Exploring Starts 假设每个回合的起始状态-动作对都可能被访问到。

### 算法步骤

- 从某个起始状态-动作对开始一个回合；
- 生成完整回合；
- 用回报更新动作值；
- 再按更优动作调整策略。

### 首次访问（First-Visit）与每次访问（Every-Visit）

- First-Visit：只用该回合中第一次访问状态的回报。
- Every-Visit：该回合里每次访问都更新。

### Exploring Starts的局限性

这个假设在很多现实问题里并不自然，因此更常用的是基于 ε-贪婪的做法。

---

## 5.5 MC ε-Greedy算法

### 核心思想

在完整回合结束后，用实际回报更新动作值，同时用 ε-贪婪保持探索。

### 算法步骤（伪代码）

```text
初始化 Q
重复生成一整个回合
对回合中的状态-动作对：
    用该时刻后的实际回报 G 更新 Q(s,a)
    让策略在该状态上对 Q 贪婪，并保留 ε 探索
```

### On-Policy vs Off-Policy

- On-Policy：学习的就是当前实际执行的那条策略。
- Off-Policy：行为策略和目标策略可以不同。

MC ε-Greedy 通常属于 on-policy 控制。

### 为什么MC ε-Greedy是最常用的MC算法？

- 不依赖 Exploring Starts 这种较强假设；
- 保留探索；
- 实现直观，适合入门。

---

## 直观理解：蒙特卡洛方法的本质

### 核心思想一句话概括

> 走完整条轨迹，再用真实结果回头修正之前的估计。

### 与动态规划方法的关系

- 动态规划依赖模型做期望；
- 蒙特卡洛依赖完整样本做平均。

### MC方法的优势与劣势

优势：

- 不需要环境模型；
- 更新目标直接来自真实回报，概念清晰。

劣势：

- 必须等回合结束才能更新；
- 方差可能较大；
- 对持续任务不够方便。

---

## 本章总结

- 蒙特卡洛方法用样本回报近似期望回报。
- 它适合无模型设定，但依赖完整回合。
- ε-贪婪是解决探索问题的常见办法。
- 本章为第7章的 TD 方法做好了对比基础。
