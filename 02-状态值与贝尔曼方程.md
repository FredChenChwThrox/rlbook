# 第2章：状态值与贝尔曼方程（State Values and Bellman Equation）

## 学习目标

- 理解状态值（state value）的定义及其直观含义
- 掌握贝尔曼方程（Bellman equation）的核心思想
- 学会推导和书写贝尔曼方程
- 理解贝尔曼方程的线性方程组本质
- 掌握贝尔曼方程的矩阵-向量形式和迭代求解方法
- 了解动作值（action value）的概念及其与状态值的关系

---

## 核心概念

本章围绕两个核心概念展开：

1. **状态值（State Value）**：评估一个策略好坏的量化指标
2. **贝尔曼方程（Bellman Equation）**：描述状态值之间关系的数学方程

---

## 2.1 动机示例1：为什么需要状态值？

### 问题：如何评估一个策略的好坏？

第1章我们学会了描述策略 $\pi(a|s)$——它告诉智能体在每个状态下应该怎么做。但一个重要的问题随之而来：**这个策略好不好？**

### 直观方法：看回报

一个自然的想法是：看智能体从这个策略中获得的**回报** $G_t$ 有多大。回报越大，策略越好。

**问题**：回报 $G_t$ 是一个**随机变量**——同一个策略在不同回合中可能得到不同的回报（因为状态转移和奖励可能随机）。

### 更好的方法：看回报的期望

为了消除随机性的影响，我们可以看**回报的期望**（Expected Return）：

$$\mathbb{E}_\pi[G_t \mid S_t = s]$$

**含义**：从状态 $s$ 出发，遵循策略 $\pi$，期望能获得多少累积折扣奖励。

这个期望值就是**状态值**（State Value）！

---

## 2.2 动机示例2：如何计算回报？

假设你想计算从某个状态 $s$ 出发，遵循策略 $\pi$ 能获得的期望回报。

**直接方法**：枚举所有可能的轨迹，计算每条轨迹的回报，然后取加权平均。

**问题**：轨迹数量可能**无穷无尽**！尤其是在随机性转移和持续式任务中。

**状态值提供了一个更优雅的解决方案**：利用**贝尔曼方程**，将计算问题转化为解方程组的问题。

---

## 2.3 状态值（State Values）

### 定义

状态值 $v_\pi(s)$ 是从状态 $s$ 出发，遵循策略 $\pi$ 能获得的**期望回报**（Expected Return）：

$$v_\pi(s) = \mathbb{E}_\pi[G_t \mid S_t = s]$$

**展开写**：

$$v_\pi(s) = \mathbb{E}_\pi\left[\sum_{k=0}^{\infty} \gamma^k R_{t+k+1} \mid S_t = s\right]$$

**各符号含义**：

| 符号 | 含义 |
|:---:|:---|
| $v_\pi(s)$ | 状态值函数（state-value function），表示从状态 $s$ 出发、遵循策略 $\pi$ 的期望回报 |
| $\mathbb{E}_\pi[\cdot]$ | 期望运算符，下标 $\pi$ 表示期望是基于策略 $\pi$ 计算的 |
| $G_t$ | 时刻 $t$ 的回报（累积折扣奖励） |
| $S_t = s$ | 时刻 $t$ 的状态是 $s$ |
| $\gamma$ | 折扣因子 |
| $R_{t+k+1}$ | 时刻 $t+k+1$ 获得的奖励 |

### 状态值的意义

- **状态值越大**，说明从这个状态出发的策略越好
- 状态值可以用来**比较不同策略**——如果对所有状态 $s$ 都有 $v_{\pi_1}(s) \geq v_{\pi_2}(s)$，则策略 $\pi_1$ 优于 $\pi_2$
- 用状态值来评估策略好坏的过程叫做**策略评估**（Policy Evaluation）

### 生活类比：位置评分

> **类比**：想象你在一座大城市的地铁站系统里。状态值 $v_\pi(s)$ 就像是给**每个地铁站打分**——分数越高，说明从那个站出发按照某条路线（策略 $\pi$）最终能到达的好地方的加权总和越高。
> 
> 比如，从"购物中心站"出发可能有很高的分数（因为附近有很多好去处），而从"偏远郊区站"出发分数可能较低。

---

## 2.4 贝尔曼方程（Bellman Equation）

贝尔曼方程是强化学习理论中最重要的方程之一。它描述了**所有状态值之间的递归关系**。

### 贝尔曼方程（基本形式）

$$v_\pi(s) = \sum_a \pi(a|s) \sum_{s'} p(s'|s,a)\left[r + \gamma v_\pi(s')\right]$$

### 直观理解

让我们逐层拆解这个公式：

```
v_π(s) = Σ_a π(a|s)  Σ_{s'} p(s'|s,a) [r + γ v_π(s')]
│          │            │              └─────────────┘
│          │            │                  │
│          │            │         即时奖励 + 折扣后的下一状态值
│          │            │
│          │         对所有可能的下一个状态求和
│          │         （按到达概率加权）
│          │
│       对所有可能的动作求和
│       （按策略概率加权）
│
当前状态的值
```

**翻译成人话**：

> **当前状态的值 = 对所有可能动作的期望 × 对所有可能下一状态的期望 ×（即时奖励 + 折扣后的下一状态值）**

更具体地说：

1. **第一步**：在状态 $s$，你要按照策略 $\pi(a|s)$ 选择一个动作 $a$
2. **第二步**：执行动作 $a$ 后，你按照转移概率 $p(s'|s,a)$ 到达下一个状态 $s'$，并获得奖励 $r$
3. **第三步**：你获得两部分收益——
   - **即时奖励** $r$（这一步直接得到的）
   - **未来收益** $\gamma v_\pi(s')$（从下一状态 $s'$ 出发、继续遵循策略 $\pi$ 的期望回报，打个折扣 $\gamma$）
4. **综合**：对所有可能的动作和下一状态取期望

### 生活类比：接力赛

> **类比**：贝尔曼方程就像一场**接力赛跑**。
> 
> 想象你（当前状态 $s$）站在起跑线上。比赛规则是：
> - 你先抛硬币决定跑哪条跑道（选择动作，按策略 $\pi$ 的概率）
> - 跑完第一棒后，你把接力棒传给下一位选手（到达下一状态 $s'$）
> - 你的**总成绩** = 你自己第一棒的成绩（即时奖励 $r$）+ 下一位选手后续所有棒次的总成绩（但他的成绩要按折扣因子 $\gamma$ 折算，因为他跑得越早贡献越小）
> 
> 贝尔曼方程就是在说：**你的总成绩 = 你第一棒的成绩 + 下一位选手成绩的打折版**。

---

### 贝尔曼方程的要点

贝尔曼方程有几个关键的特性需要理解：

| 特性 | 说明 |
|:---:|:---|
| **未知量** | $v_\pi(s)$ 和 $v_\pi(s')$ 是未知的状态值（需要求解） |
| **已知量** | $\pi(a|s)$ 是给定的策略；$p(s'\|s,a)$ 和 $r$ 是环境模型（已知） |
| **线性方程组** | 每个状态 $s$ 对应一个方程，所有方程联立构成线性方程组 |
| **不是单个方程** | 贝尔曼方程是一组方程，状态数有多少就有多少个方程 |

> **注意**：你可能会想——"方程两边都有 $v_\pi$，这怎么解？" 答案是：对每个状态写一个方程，联立成方程组来解！这就是贝尔曼方程的精妙之处。

---

### 等价形式1：包含奖励概率的形式

在某些情况下，奖励也是随机的（依赖于 $s, a, s'$）。此时贝尔曼方程可以写成：

$$v_\pi(s) = \sum_a \pi(a|s) \sum_{s'} \sum_r p(s', r|s,a)\left[r + \gamma v_\pi(s')\right]$$ 

**与基本形式的区别**：这里用联合概率 $p(s', r|s,a)$ 代替了 $p(s'|s,a)$，并对奖励 $r$ 也求和。

**含义**：
- $p(s', r|s,a)$：从状态 $s$ 执行动作 $a$ 后，到达状态 $s'$ 且获得奖励 $r$ 的联合概率
- 对 $s'$ 和 $r$ 双重求和，覆盖所有可能的下一状态和奖励组合

---

### 等价形式2：奖励仅依赖于下一状态

在很多实际问题中，奖励只取决于到达的下一状态（而与具体如何到达无关）。此时：

$$v_\pi(s) = \sum_a \pi(a|s) \sum_{s'} p(s'|s,a)\left[r(s') + \gamma v_\pi(s')\right]$$ 

**这里的区别**：奖励写成 $r(s')$ 而非 $r$，表示奖励只取决于下一状态 $s'$。

**示例**：在网格世界中，到达目标格子就获得 +1 奖励，进入禁区获得 -1 奖励——这些奖励只与到达的格子（状态）有关，与从哪个方向来无关。

---

## 2.5 贝尔曼方程示例

让我们通过网格世界的例子来具体写出贝尔曼方程。

### 设定

假设一个简单的网格世界（确定性转移，确定性策略）：

- 策略 $\pi$：总是选择向右走（如果可能的话）
- 奖励规则：到达目标 $s_G$ 获得 $r = +1$，其余 $r = 0$
- 折扣因子 $\gamma = 0.9$

### 写出每个状态的贝尔曼方程

对于每个状态 $s$，我们有：

$$v_\pi(s) = r + \gamma v_\pi(s')$$

**示例计算**（假设确定性转移）：

| 状态 | 下一状态 | 即时奖励 | 贝尔曼方程 |
|:---:|:---:|:---:|:---|
| $s_1$ | $s_2$ | 0 | $v_\pi(s_1) = 0 + 0.9 \cdot v_\pi(s_2)$ |
| $s_2$ | $s_3$ | 0 | $v_\pi(s_2) = 0 + 0.9 \cdot v_\pi(s_3)$ |
| $s_3$ | $s_G$ | 0 | $v_\pi(s_3) = 0 + 0.9 \cdot v_\pi(s_G)$ |
| $s_G$ | $s_G$ | +1 | $v_\pi(s_G) = 1 + 0.9 \cdot v_\pi(s_G)$ |

**求解**：

从最后一个方程开始：

$$v_\pi(s_G) = 1 + 0.9 \cdot v_\pi(s_G)$$

$$v_\pi(s_G) - 0.9 \cdot v_\pi(s_G) = 1$$

$$0.1 \cdot v_\pi(s_G) = 1$$

$$v_\pi(s_G) = 10$$

然后回代：

$$v_\pi(s_3) = 0.9 \cdot 10 = 9$$

$$v_\pi(s_2) = 0.9 \cdot 9 = 8.1$$

$$v_\pi(s_1) = 0.9 \cdot 8.1 = 7.29$$

**结果解读**：
- 离目标越远，状态值越小
- 状态值反映了"这个位置离目标有多远"（以累积折扣奖励衡量）

---

## 2.6 贝尔曼方程的矩阵-向量形式

贝尔曼方程可以更紧凑地写成**矩阵-向量形式**，这对于理论分析和数值求解非常有用。

### 矩阵形式

$$v_\pi = r_\pi + \gamma P_\pi v_\pi$$

**各符号含义**：

| 符号 | 维度 | 含义 |
|:---:|:---:|:---|
| $v_\pi$ | $|\mathcal{S}| \times 1$ | 状态值**向量**，第 $i$ 个元素是 $v_\pi(s_i)$ |
| $r_\pi$ | $|\mathcal{S}| \times 1$ | 期望奖励**向量**，第 $i$ 个元素是从 $s_i$ 出发的期望即时奖励 |
| $P_\pi$ | $|\mathcal{S}| \times |\mathcal{S}|$ | 状态转移**矩阵**，元素 $(i,j)$ 表示从 $s_i$ 到 $s_j$ 的转移概率 |
| $\gamma$ | 标量 | 折扣因子 |
| $I$ | $|\mathcal{S}| \times |\mathcal{S}|$ | 单位矩阵 |

### 如何构建 $r_\pi$ 和 $P_\pi$

$$r_\pi(s) = \sum_a \pi(a|s) \sum_{s'} p(s'|s,a) \cdot r(s,a,s')$$

$$P_\pi(s,s') = \sum_a \pi(a|s) \cdot p(s'|s,a)$$

**含义**：
- $r_\pi(s)$：从状态 $s$ 出发、按照策略 $\pi$ 的**期望即时奖励**
- $P_\pi(s,s')$：从状态 $s$ 按照策略 $\pi$ 转移到 $s'$ 的**总概率**

---

## 2.7 求解贝尔曼方程

### 方法1：闭式解（Closed-form Solution）

从矩阵形式出发：

$$v_\pi = r_\pi + \gamma P_\pi v_\pi$$

将含 $v_\pi$ 的项移到左边：

$$v_\pi - \gamma P_\pi v_\pi = r_\pi$$

$$(I - \gamma P_\pi) v_\pi = r_\pi$$

当矩阵 $(I - \gamma P_\pi)$ 可逆时（对于 $\gamma < 1$，它总是可逆的）：

$$v_\pi = (I - \gamma P_\pi)^{-1} r_\pi$$

**优缺点**：
- ✅ 直接得到精确解
- ❌ 计算复杂度 $O(|\mathcal{S}|^3)$，状态数很大时不可行

### 方法2：迭代解（Iterative Solution）

更实用的方法是**迭代求解**：

$$v_{k+1} = r_\pi + \gamma P_\pi v_k$$

**算法步骤**：

1. **初始化**：任意设置 $v_0$（例如全零向量）
2. **迭代更新**：用上面的公式反复计算 $v_1, v_2, v_3, \ldots$
3. **收敛判断**：当 $\|v_{k+1} - v_k\| < \epsilon$（某个很小的阈值）时停止
4. **返回结果**：$v_k$ 就是 $v_\pi$ 的近似解

**为什么迭代方法会收敛？**

当 $\gamma < 1$ 时，迭代算子是一个**收缩映射**（contraction mapping），每次迭代都将解向真实值拉近。理论上可以证明迭代法一定收敛到唯一解。

**优缺点**：
- ✅ 计算复杂度每步 $O(|\mathcal{S}|^2)$，更易于扩展
- ✅ 可以灵活设置精度，在精度和效率间权衡
- ✅ 是后续学习算法（如值迭代）的基础

### 直观理解：迭代法就像渐进逼近

> **类比**：迭代求解状态值就像是用望远镜逐渐对焦——一开始画面模糊（初始猜测 $v_0$），每次转动对焦环（迭代更新），画面就清晰一点，最终完全清晰（收敛到真实值 $v_\pi$）。

---

## 2.8 从状态值到动作值（Action Values）

状态值 $v_\pi(s)$ 告诉我们"从状态 $s$ 出发、遵循策略 $\pi$ 有多好"。但它没有告诉我们"在状态 $s$ 下**具体采取某个动作 $a$** 有多好"。

为此，我们引入**动作值**（Action Value）。

### 动作值的定义

动作值 $q_\pi(s,a)$ 表示：在状态 $s$ 下采取动作 $a$，然后**此后遵循策略 $\pi$** 的期望回报：

$$q_\pi(s,a) = \sum_{s'} p(s'|s,a)\left[r + \gamma v_\pi(s')\right]$$

**另一种写法**（当奖励仅依赖于 $s$ 和 $a$ 时）：

$$q_\pi(s,a) = r(s,a) + \gamma \sum_{s'} p(s'|s,a) \cdot v_\pi(s')$$

### 状态值与动作值的关系

状态值是所有动作值的**策略加权平均**：

$$v_\pi(s) = \sum_a \pi(a|s) \cdot q_\pi(s,a)$$

**含义**：一个状态的值 = 按照策略选择动作的期望 × 每个动作的值

### 动作值的意义

动作值非常重要，因为：

1. **评估具体行为**：它告诉我们"在某个状态下做某个具体动作"的好坏
2. **改进策略**：如果 $q_\pi(s, a_1) > q_\pi(s, a_2)$，说明在状态 $s$ 下动作 $a_1$ 比 $a_2$ 更好
3. **策略改进的基础**：第3章将看到，动作值是找到最优策略的关键

### 生活类比：动作值的具体选择

> **类比**：状态值就像是给**城市打分**（从哪个城市出发旅行体验最好）。动作值则是给**具体的出行选择**打分——"从北京出发，坐高铁去上海"得分多少，"从北京出发，坐飞机去广州"得分多少。
> 
> 状态值是"站在北京"的整体期望值（综合考虑各种出行方式的概率），而动作值则是"从北京坐高铁去上海"这个**具体选择**的期望值。

---

## 本章总结

### 知识脉络图

```
第1章：基本概念（MDP框架）
    │
    ▼
第2章：评估策略好坏
    ├── 状态值 v_π(s) = E_π[G_t | S_t = s]
    │       从状态s出发、遵循策略π的期望回报
    │
    ├── 贝尔曼方程：描述状态值之间的递归关系
    │       v_π(s) = Σ_a π(a|s) Σ_{s'} p(s'|s,a)[r + γv_π(s')]
    │       
    ├── 求解方法
    │       ├── 闭式解：v_π = (I - γP_π)^(-1) r_π
    │       └── 迭代解：v_{k+1} = r_π + γP_π v_k
    │
    └── 动作值 q_π(s,a)
            在状态s下采取动作a、然后遵循策略π的期望回报
            
            ▼
第3章：寻找最优策略
```

### 关键公式速查

| 概念 | 公式 | 含义 |
|:---:|:---:|:---|
| 状态值定义 | $v_\pi(s) = \mathbb{E}_\pi[G_t \| S_t = s]$ | 期望回报 |
| 贝尔曼方程（基本形式） | $v_\pi(s) = \sum_a \pi(a\|s) \sum_{s'} p(s'\|s,a)[r + \gamma v_\pi(s')]$ | 状态值的递归关系 |
| 贝尔曼方程（含奖励概率） | $v_\pi(s) = \sum_a \pi(a\|s) \sum_{s'}\sum_r p(s',r\|s,a)[r + \gamma v_\pi(s')]$ | 奖励随机时 |
| 贝尔曼方程（矩阵形式） | $v_\pi = r_\pi + \gamma P_\pi v_\pi$ | 紧凑的向量形式 |
| 闭式解 | $v_\pi = (I - \gamma P_\pi)^{-1} r_\pi$ | 直接求解 |
| 迭代解 | $v_{k+1} = r_\pi + \gamma P_\pi v_k$ | 逐步逼近 |
| 动作值 | $q_\pi(s,a) = \sum_{s'} p(s'\|s,a)[r + \gamma v_\pi(s')]$ | 评估具体动作 |
| 状态值与动作值关系 | $v_\pi(s) = \sum_a \pi(a\|s) q_\pi(s,a)$ | 策略加权平均 |

---

## 关键概念回顾

| 概念 | 英文 | 符号 | 核心含义 |
|:---:|:---:|:---:|:---|
| 状态值 | State Value | $v_\pi(s)$ | 从状态 $s$ 出发、遵循策略 $\pi$ 的期望回报 |
| 策略评估 | Policy Evaluation | — | 用状态值评估策略好坏的过程 |
| 贝尔曼方程 | Bellman Equation | — | 描述状态值之间递归关系的线性方程组 |
| 状态值向量 | State Value Vector | $v_\pi$ | 所有状态值组成的向量 |
| 期望奖励向量 | Expected Reward Vector | $r_\pi$ | 各状态的期望即时奖励 |
| 状态转移矩阵 | State Transition Matrix | $P_\pi$ | 按策略的状态转移概率矩阵 |
| 闭式解 | Closed-form Solution | $v_\pi = (I-\gamma P_\pi)^{-1}r_\pi$ | 直接矩阵求逆求解 |
| 迭代解 | Iterative Solution | $v_{k+1} = r_\pi + \gamma P_\pi v_k$ | 逐步逼近真实值 |
| 动作值 | Action Value | $q_\pi(s,a)$ | 在状态 $s$ 采取动作 $a$ 的好坏评估 |
