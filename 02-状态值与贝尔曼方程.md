# 第2章：状态值与贝尔曼方程（State Values and Bellman Equation）

## 学习目标

- 理解状态值和动作值的定义。
- 掌握贝尔曼方程的核心含义。
- 知道如何把策略评估写成方程组或迭代过程。

---

## 核心概念

本章有两个中心问题：

1. 给定一个策略，怎样评价它？
2. 这种评价为什么能写成递归方程？

---

## 2.1 动机示例1：为什么需要状态值？

### 问题：如何评估一个策略的好坏？

只知道策略 $\pi$ 还不够，我们还需要知道：

> 从某个状态出发，按照这个策略继续走，未来平均能拿到多少总收益？

### 直观方法：看回报

最直接的想法是看回报 $G_t$。

### 更好的方法：看回报的期望

因为单次回报可能有随机性，所以更稳定的量是它的期望：

$$\mathbb{E}_\pi[G_t \mid S_t=s]$$

这个期望就是状态值的核心出发点。

---

## 2.2 动机示例2：如何计算回报？

直接枚举所有可能轨迹通常不现实，因为未来路径太多。

更好的做法是把“当前状态的价值”拆成两部分：

- 当前一步的奖励
- 下一状态未来还能带来的价值

这正是贝尔曼方程的思路。

---

## 2.3 状态值（State Values）

### 定义

状态值函数定义为：

$$v_\pi(s) = \mathbb{E}_\pi[G_t \mid S_t=s]$$

展开后可写成：

$$v_\pi(s) = \mathbb{E}_\pi\left[\sum_{k=0}^{\infty}\gamma^k R_{t+k+1} \mid S_t=s\right]$$

白话解释：

- $v_\pi(s)$ 表示“从状态 $s$ 出发，之后一直按策略 $\pi$ 行动，平均能得到多少长期收益”。
- 它不是某一次试验的结果，而是平均意义下的长期价值。

### 状态值的意义

- 值越大，说明这个状态在该策略下越“有前途”。
- 它常用于比较不同策略的优劣。

### 生活类比：位置评分

可以把状态值理解为给“当前位置”打分。

- 分数高：从这里出发往往会走向更好的未来。
- 分数低：从这里出发未来回报通常不理想。

---

## 2.4 贝尔曼方程（Bellman Equation）

### 贝尔曼方程（基本形式）

$$v_\pi(s) = \sum_a \pi(a|s)\sum_{s'} p(s'|s,a)\left[r + \gamma v_\pi(s')\right]$$

### 直观理解

这个式子的意思可以拆成四步：

1. 在状态 $s$ 下，策略会按 $\pi(a|s)$ 选择动作。
2. 每个动作又会按 $p(s'|s,a)$ 到达不同下一状态。
3. 每种结果都带来“即时奖励 $r$ + 折扣后的下一状态价值 $\gamma v_\pi(s')$”。
4. 对所有可能结果做加权平均，就得到当前状态的价值。

一句白话：

> 当前状态的价值 = 这一步先拿到的奖励 + 下一步以后还能拿到的价值。

### 生活类比：接力赛

当前选手跑完这一棒拿到即时成绩，下一棒还能继续累积成绩。贝尔曼方程就是把“整场比赛总成绩”写成“当前这一棒 + 后面所有棒”的形式。

### 贝尔曼方程的要点

- 它是递归方程。
- 它把值函数之间联系起来。
- 它是后面动态规划、TD、Q-Learning 等方法的基础。

### 等价形式1：包含奖励概率的形式

有时会把奖励和下一状态一起写进联合概率中，本质上仍然是在做期望。

### 等价形式2：奖励仅依赖于下一状态

如果环境设定更简单，公式也可以相应简化。学习时关键不是记所有变体，而是理解“期望 + 递归”的结构。

---

## 2.5 贝尔曼方程示例

### 设定

在小型网格世界里，每个状态都能写出一个对应方程。

### 写出每个状态的贝尔曼方程

做法通常是：

- 先列出该状态下策略会怎么选动作；
- 再列出各动作可能转移到哪些状态；
- 然后把即时奖励和下一状态值代进去。

学习提示：

- 这一节最适合手算一个很小的例子。
- 手算过一次后，后面算法中的更新公式会更容易理解。

---

## 2.6 贝尔曼方程的矩阵-向量形式

### 矩阵形式

在有限状态空间里，贝尔曼方程可写成：

$$v_\pi = r_\pi + \gamma P_\pi v_\pi$$

白话解释：

- $v_\pi$ 是所有状态值组成的列向量；
- $r_\pi$ 是各状态下一步期望奖励；
- $P_\pi$ 是在策略 $\pi$ 下的状态转移矩阵。

这个式子说明：整个策略评估问题可以看成一个线性方程组。

### 如何构建 $r_\pi$ 和 $P_\pi$

- $r_\pi$：把每个状态下的期望即时奖励写出来。
- $P_\pi$：把策略和环境转移概率合并成一个状态到状态的矩阵。

---

## 2.7 求解贝尔曼方程

### 方法1：闭式解（Closed-form Solution）

由

$$v_\pi = r_\pi + \gamma P_\pi v_\pi$$

可得

$$v_\pi = (I-\gamma P_\pi)^{-1}r_\pi$$

白话解释：

- 这相当于一次性把整组状态值解出来。
- 但当状态很多时，直接求逆往往代价太大。

### 方法2：迭代解（Iterative Solution）

更常见的做法是反复更新：

$$v_{k+1}(s) \leftarrow \sum_a \pi(a|s)\sum_{s'} p(s'|s,a)\left[r + \gamma v_k(s')\right]$$

### 直观理解：迭代法就像渐进逼近

- 先随便给每个状态一个初值；
- 每轮都用贝尔曼方程重新修正；
- 修正多次后，值会逐渐稳定下来。

---

## 2.8 从状态值到动作值（Action Values）

### 动作值的定义

动作值函数定义为：

$$q_\pi(s,a) = \mathbb{E}_\pi[G_t \mid S_t=s, A_t=a]$$

白话解释：

- 状态值问的是“在这个状态下总体有多好”；
- 动作值问的是“在这个状态先做这个动作到底值不值”。

### 状态值与动作值的关系

$$v_\pi(s) = \sum_a \pi(a|s) q_\pi(s,a)$$

意思是：状态值是该状态下各动作值的策略加权平均。

### 动作值的意义

动作值在“策略改进”里非常关键，因为它直接告诉我们在当前状态下哪个动作更值得选。

### 生活类比：动作值的具体选择

状态值像“这个路口整体位置好不好”，动作值像“在这个路口向左还是向右更值”。

---

## 本章总结

### 知识脉络图

- 回报是长期收益。
- 状态值是回报的期望。
- 贝尔曼方程把值函数写成递归关系。
- 动作值进一步把“状态”和“动作”绑定起来。

### 关键公式速查

状态值：

$$v_\pi(s) = \mathbb{E}_\pi[G_t \mid S_t=s]$$

贝尔曼方程：

$$v_\pi(s) = \sum_a \pi(a|s)\sum_{s'} p(s'|s,a)\left[r + \gamma v_\pi(s')\right]$$

矩阵形式：

$$v_\pi = r_\pi + \gamma P_\pi v_\pi$$

动作值：

$$q_\pi(s,a) = \mathbb{E}_\pi[G_t \mid S_t=s, A_t=a]$$

## 关键概念回顾

- 状态值：评价一个状态在给定策略下的长期好坏。
- 贝尔曼方程：用“一步奖励 + 下一步价值”描述当前价值。
- 动作值：用于判断哪个动作更优。
