# 附录：数学预备知识

> 本附录整理了学习强化学习所需的数学基础，包括**概率论**、**期望**、**梯度下降**和**收敛性**四个部分。每个概念都配有直观解释和生活类比，力求让数学基础薄弱的读者也能理解核心思想。

---

## A. 概率论基础（Probability Theory）

### A.1 随机变量（Random Variable）

**定义**：随机变量是一个其取值由随机试验结果决定的变量。

**表示方法**：
- **大写字母**（如 $X, Y, S, A, R$）：表示随机变量本身
- **小写字母**（如 $x, y, s, a, r$）：表示随机变量的具体取值

**生活类比**：

> 掷骰子：$X$ 是随机变量（代表掷骰子的结果），$x = 4$ 是 $X$ 的一次具体取值（这次掷出了4点）。
>
> 强化学习中：$S_t$ 是时刻 $t$ 的状态随机变量，$s_5$ 是某个具体的状态编号5。

### A.2 概率（Probability）

**概率的定义**：事件 $X = x$ 发生的概率，记作：

$$p(X = x) \quad \text{或简写为} \quad p(x)$$

**概率的性质**：

$$0 \leq p(x) \leq 1, \quad \sum_{x} p(x) = 1$$

**强化学习中的应用**：

$$\pi(a|s) = \text{在状态}~s~\text{下选择动作}~a~\text{的概率}$$

$$p(s'|s,a) = \text{在状态}~s~\text{执行动作}~a~\text{后转移到}~s'~\text{的概率}$$

### A.3 联合概率（Joint Probability）

两个随机变量同时取特定值的概率：

$$p(X = x, Y = y) \quad \text{或简写为} \quad p(x, y)$$

**例子**：

> 从一副扑克中抽一张牌：
> - $p(\text{红桃}, \text{K})$ = 抽到红桃K的概率 = 1/52
> - $p(\text{红色}, \text{偶数})$ = 抽到红色偶数牌的概率

**联合概率的性质**：

$$\sum_{x} \sum_{y} p(x, y) = 1$$

### A.4 边缘概率（Marginal Probability）

从联合概率中得到单个变量的概率：

$$p(x) = \sum_{y} p(x, y)$$

$$p(y) = \sum_{x} p(x, y)$$

**直观理解**：

> 联合概率是一张二维表格，边缘概率是把表格**按行或列相加**得到的边缘值。

**例子**（天气和活动）：

| | 室内 | 室外 | 边缘（天气） |
|--|------|------|------------|
| 晴天 | 0.1 | 0.4 | **0.5** |
| 雨天 | 0.3 | 0.2 | **0.5** |
| 边缘（活动） | **0.4** | **0.6** | 1.0 |

$$p(\text{晴天}) = p(\text{晴天}, \text{室内}) + p(\text{晴天}, \text{室外}) = 0.1 + 0.4 = 0.5$$

### A.5 条件概率（Conditional Probability）

在已知 $Y = y$ 的条件下，$X = x$ 的概率：

$$p(x|y) = \frac{p(x, y)}{p(y)}$$

**条件概率的直观理解**：

> 已知今天下雨，你带伞的概率是多少？这就是条件概率——**缩小了考虑范围**后重新评估概率。

**强化学习中的应用**：

$$\pi(a|s) = \frac{p(s, a)}{p(s)} = \text{在状态}~s~\text{下选}~a~\text{的概率}$$

### A.6 全概率公式（Law of Total Probability）

$$p(x) = \sum_{y} p(x|y) p(y)$$

**直观理解**：

> 想知道今天下雨的总概率？考虑所有可能的天气系统，分别计算"在这种系统影响下雨的概率 × 这种系统出现的概率"，然后全部相加。

**生活例子**：

> 一个工厂有三条生产线（A、B、C），各自的生产比例和次品率不同。总次品率 = 各条线的次品率 × 各条线的产量占比，再加总。

### A.7 贝叶斯公式（Bayes' Theorem）

$$p(x|y) = \frac{p(y|x) p(x)}{p(y)}$$

其中：
- $p(x)$：**先验概率（Prior）** — 在观察数据前的初始信念
- $p(y|x)$：**似然（Likelihood）** — 在假设成立下观察到数据的概率
- $p(x|y)$：**后验概率（Posterior）** — 在观察数据后的更新信念
- $p(y)$：**证据（Evidence）** — 观察到数据的总概率

**经典例子：医疗检测**

> 某种疾病在人群中的患病率 $p(\text{病}) = 0.001$。检测的准确率：
> - 有病的人检测阳性率 $p(\text{阳性}|\text{病}) = 0.99$
> - 没病的人检测阴性率 $p(\text{阴性}|\text{无病}) = 0.99$
>
> 如果一个人检测为阳性，他真的患病的概率是多少？
>
> $$p(\text{病}|\text{阳性}) = \frac{p(\text{阳性}|\text{病}) \cdot p(\text{病})}{p(\text{阳性})} = \frac{0.99 \times 0.001}{0.99 \times 0.001 + 0.01 \times 0.999} \approx 0.09$$
>
> 只有约9%！尽管检测很准，但因为患病率低，假阳性仍然很多。

### A.8 概率分布速查表

| 分布 | 符号 | 概率/密度函数 | 均值 | 方差 |
|------|------|-------------|------|------|
| 均匀分布 | $\mathcal{U}(a,b)$ | $\frac{1}{b-a}$ | $\frac{a+b}{2}$ | $\frac{(b-a)^2}{12}$ |
| 正态/高斯分布 | $\mathcal{N}(\mu, \sigma^2)$ | $\frac{1}{\sqrt{2\pi}\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$ | $\mu$ | $\sigma^2$ |
| 伯努利分布 | $\text{Bernoulli}(p)$ | $p^x(1-p)^{1-x}, x \in \{0,1\}$ | $p$ | $p(1-p)$ |
| 二项分布 | $\text{Binomial}(n,p)$ | $\binom{n}{k} p^k(1-p)^{n-k}$ | $np$ | $np(1-p)$ |

---

## B. 期望（Expectation）

### B.1 期望的定义

随机变量 $X$ 的期望（平均值、均值）：

$$\mathbb{E}[X] = \sum_{x} x \cdot p(x)$$

**连续情况**：

$$\mathbb{E}[X] = \int_{-\infty}^{+\infty} x \cdot p(x) \, dx$$

**直观理解**：

> 期望就是"加权平均"——每个可能取值乘上它发生的概率，全部加起来。
>
> 掷骰子的期望：$\mathbb{E}[X] = 1 \times \frac{1}{6} + 2 \times \frac{1}{6} + 3 \times \frac{1}{6} + 4 \times \frac{1}{6} + 5 \times \frac{1}{6} + 6 \times \frac{1}{6} = 3.5$

### B.2 函数的期望

$$\mathbb{E}[f(X)] = \sum_{x} f(x) \cdot p(x)$$

**强化学习中的应用**：

$$\mathbb{E}_{\pi}[G_t | S_t = s] = v_{\pi}(s) \quad \text{——状态值函数就是回报的期望}$$

### B.3 期望的线性性

期望最重要的性质：

$$\mathbb{E}[aX + bY] = a\mathbb{E}[X] + b\mathbb{E}[Y]$$

其中 $a, b$ 是常数。注意：**不要求 $X$ 和 $Y$ 独立**！

**应用示例**：

> 假设你投资了两种股票，A和B。无论你用哪种投资组合，总收益的期望等于各自期望的加权和。

### B.4 条件期望

在 $Y = y$ 条件下，$X$ 的期望：

$$\mathbb{E}[X | Y = y] = \sum_{x} x \cdot p(x|y)$$

**全期望公式**：

$$\mathbb{E}[X] = \sum_{y} \mathbb{E}[X | Y = y] \cdot p(y)$$

**强化学习中的应用**：

$$v_{\pi}(s) = \mathbb{E}_{\pi}[G_t | S_t = s] = \sum_{a} \pi(a|s) \cdot \mathbb{E}[G_t | S_t = s, A_t = a]$$

### B.5 方差与标准差

**方差**衡量随机变量偏离期望的程度：

$$\text{Var}(X) = \mathbb{E}\big[(X - \mathbb{E}[X])^2\big] = \mathbb{E}[X^2] - \big(\mathbb{E}[X]\big)^2$$

**标准差**：

$$\sigma_X = \sqrt{\text{Var}(X)}$$

**强化学习中的应用**：

- **偏差（Bias）**：期望估计与真实值的差异——$\mathbb{E}[\hat{\theta}] - \theta$
- **方差（Variance）**：估计值的波动程度——$\text{Var}(\hat{\theta})$
- **偏差-方差权衡（Bias-Variance Tradeoff）**：减小偏差可能增大方差，反之亦然。强化学习中TD方法偏差小方差大，MC方法偏差大方差小。

---

## C. 梯度下降预备知识

### C.1 梯度（Gradient）

**梯度定义**：函数 $f(\mathbf{x})$ 对向量 $\mathbf{x}$ 的偏导数组成的向量：

$$\nabla_{\mathbf{x}} f(\mathbf{x}) = \begin{bmatrix} \frac{\partial f}{\partial x_1} \\ \frac{\partial f}{\partial x_2} \\ \vdots \\ \frac{\partial f}{\partial x_n} \end{bmatrix}$$

**梯度的几何意义**：

> **梯度指向函数增长最快的方向**，其大小表示增长的速率。
>
> 类比：站在山上，梯度指向最陡的上坡方向。梯度的反方向就是下山最快的方向。

### C.2 梯度下降（Gradient Descent）

为了最小化函数 $f(\mathbf{x})$，沿梯度的反方向更新参数：

$$\mathbf{x}_{k+1} = \mathbf{x}_k - \alpha \nabla_{\mathbf{x}} f(\mathbf{x}_k)$$

其中 $\alpha > 0$ 是**学习率（Learning Rate）**或**步长（Step Size）**。

**直观理解**：

> 想象你在山上迷路了，想走到山谷（最低点）。梯度下降就是：每走一步，看看脚下哪个方向最陡，然后朝反方向（下坡方向）走一步。步长 $\alpha$ 就是每一步迈多大。

**步长选择的影响**：

| 步长 | 结果 | 图示 |
|------|------|------|
| 太小 | 收敛极慢 | 🔽 很多步才到谷底 |
| 合适 | 快速收敛 | ↘️ 稳步下降 |
| 太大 | 震荡或发散 | ↗️↘️ 来回跳甚至越界 |

### C.3 随机梯度下降（Stochastic Gradient Descent, SGD）

**问题**：梯度下降每次要用全部数据计算梯度，计算量大。

**解决**：用**单个样本**（或小批量样本）的梯度代替全梯度：

$$\mathbf{x}_{k+1} = \mathbf{x}_k - \alpha \nabla_{\mathbf{x}} f_i(\mathbf{x}_k)$$

其中 $f_i$ 是第 $i$ 个样本的损失函数。

**SGD的优势**：
- **计算快**：每次只需一个样本
- **内存小**：不需要存全部数据
- **可能跳出局部最优**：随机噪声有助于逃离局部极小值

**SGD的劣势**：
- **震荡**：每次更新方向不确定
- **需要调学习率**：太大会发散，太小收敛慢

### C.4 小批量梯度下降（Mini-batch Gradient Descent）

折中方案：每次用一小批（mini-batch）样本来计算梯度：

$$\mathbf{x}_{k+1} = \mathbf{x}_k - \alpha \cdot \frac{1}{m} \sum_{i=1}^{m} \nabla_{\mathbf{x}} f_i(\mathbf{x}_k)$$

其中 $m$ 是批量大小（通常 $m = 32, 64, 128$ 等）。

| 方法 | 批量大小 | 优点 | 缺点 |
|------|---------|------|------|
| 批量梯度下降 | $m = N$（全部） | 稳定，准确梯度 | 慢，内存大 |
| 小批量梯度下降 | $m = 32 \sim 256$ | 平衡速度和稳定性 | 需要调批量大小 |
| 随机梯度下降 | $m = 1$ | 最快，内存最小 | 噪声大，不稳定 |

### C.5 强化学习中的梯度

**值函数近似的梯度**：

$$\nabla_{\mathbf{w}} \hat{v}(s, \mathbf{w}) = \text{预测值对参数的梯度}$$

线性近似时：

$$\hat{v}(s, \mathbf{w}) = \mathbf{w}^\top \boldsymbol{\phi}(s) \quad \Rightarrow \quad \nabla_{\mathbf{w}} \hat{v}(s, \mathbf{w}) = \boldsymbol{\phi}(s)$$

**策略梯度**：

$$\nabla_{\boldsymbol{\theta}} \ln \pi(a|s, \boldsymbol{\theta}) = \text{对数策略的梯度}$$

这就是策略梯度中的**得分函数（Score Function）**。

### C.6 链式法则（Chain Rule）

复合函数的求导法则：

$$\frac{\partial f(g(x))}{\partial x} = \frac{\partial f}{\partial g} \cdot \frac{\partial g}{\partial x}$$

**多变量形式**：

$$\frac{\partial f(g_1(x), g_2(x))}{\partial x} = \frac{\partial f}{\partial g_1} \frac{\partial g_1}{\partial x} + \frac{\partial f}{\partial g_2} \frac{\partial g_2}{\partial x}$$

**强化学习中的应用**：

神经网络的反向传播（Backpropagation）完全依赖链式法则。每一层的梯度通过链式法则从输出层传递到输入层。

---

## D. 序列收敛性（Convergence）

### D.1 收敛的定义

**数列收敛**：数列 $\{x_n\}$ 收敛到极限 $x^*$，记作：

$$\lim_{n \to \infty} x_n = x^*$$

定义为：对于任意 $\epsilon > 0$，存在 $N$，使得当 $n > N$ 时，$|x_n - x^*| < \epsilon$。

**直观理解**：

> 数列最终**无限接近**某个固定值。就像一个不断修正的瞄准过程，最终稳定在靶心附近。

### D.2 压缩映射定理（Contraction Mapping Theorem）

这是强化学习理论中最重要的数学定理之一。

**压缩映射（Contraction Mapping）**：

映射 $T$ 是压缩映射，如果存在一个常数 $\gamma \in [0, 1)$，使得：

$$\|T(x) - T(y)\| \leq \gamma \|x - y\| \quad \text{对所有}~x, y~\text{成立}$$

> 直观理解：$T$ 把任意两点的距离**至少压缩**了 $\gamma$ 倍（$\gamma < 1$）。

**压缩映射定理**：

如果 $T$ 是一个完备度量空间上的压缩映射，则：
1. $T$ 有**唯一的不动点** $x^*$，满足 $T(x^*) = x^*$
2. 对任意初始值 $x_0$，迭代 $x_{k+1} = T(x_k)$ 必然**收敛到** $x^*$

**强化学习中的应用——贝尔曼算子**：

贝尔曼期望算子 $\mathcal{T}^{\pi}$ 是一个**压缩映射**（压缩因子为 $\gamma$）：

$$\|\mathcal{T}^{\pi}V_1 - \mathcal{T}^{\pi}V_2\|_{\infty} \leq \gamma \|V_1 - V_2\|_{\infty}$$

**意义**：

> 这保证了：
> 1. 值函数存在**唯一解**
> 2. 迭代策略评估（动态规划）**必然收敛**
>
> 这就是为什么贝尔曼方程可以通过不断迭代求解的理论基础。

### D.3 马尔可夫性质与收敛

**马尔可夫链的收敛**：

在合适的条件下（如遍历性），马尔可夫链的状态分布会收敛到**平稳分布（Stationary Distribution）**：

$$\lim_{t \to \infty} p(S_t = s) = \mu(s)$$

**强化学习意义**：策略 $\pi$ 下状态访问频率会趋于稳定，这是策略梯度定理成立的基础。

### D.4 随机逼近（Stochastic Approximation）

**Robbins-Monro条件**（保证随机迭代收敛）：

对于更新 $w_{k+1} = w_k + \alpha_k (F(w_k) - w_k + \eta_k)$，其中 $\eta_k$ 是噪声，收敛需要：

$$\sum_{k=0}^{\infty} \alpha_k = \infty, \quad \sum_{k=0}^{\infty} \alpha_k^2 < \infty$$

**满足条件的步长**：

$$\alpha_k = \frac{1}{k} \quad \text{或} \quad \alpha_k = \frac{a}{b + k}$$

**直观理解**：

> 第一个条件保证"走得足够远"，能到达目标。
> 第二个条件保证"步长逐渐减小"，最终稳定下来。

### D.5 强化学习中的收敛性总结

| 算法 | 表格法 | 线性函数近似 | 非线性（神经网络） |
|------|--------|-------------|-----------------|
| TD(0) | ✅ 收敛到 $v_\pi$ | ✅ 收敛 | ❌ 可能发散 |
| Sarsa | ✅ 收敛到 $q_\pi$ | ✅ 收敛 | ❌ 可能发散 |
| Q-Learning | ✅ 收敛到 $q_*$ | ✅ 收敛（弱条件）| ❌ 可能发散 |
| 策略梯度 | ✅ 收敛到局部最优 | ✅ 收敛到局部最优 | ✅ 收敛到局部最优 |
| Actor-Critic | ✅ 收敛 | ✅ 收敛 | ⚠️ 需要特殊技巧 |

> 注意："可能发散"并不意味着一定发散，只是没有理论保证。在实践中，DQN和PPO等算法通过经验回放、目标网络、裁剪等技术大大改善了稳定性。

---

## E. 向量与矩阵基础

### E.1 向量和矩阵运算

**内积（点积）**：

$$\mathbf{x}^\top \mathbf{y} = \sum_{i=1}^{n} x_i y_i$$

**矩阵-向量乘法**：

$$(A\mathbf{x})_i = \sum_{j=1}^{n} A_{ij} x_j$$

### E.2 激活函数

| 激活函数 | 公式 | 图像特征 | 用途 |
|---------|------|---------|------|
| Sigmoid | $\sigma(x) = \frac{1}{1 + e^{-x}}$ | S形，输出(0,1) | 二分类输出 |
| Tanh | $\tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}$ | S形，输出(-1,1) | 隐藏层 |
| ReLU | $\text{ReLU}(x) = \max(0, x)$ | 负半轴为零，正半轴线性 | 隐藏层（最常用）|
| Softmax | $\text{softmax}(x_i) = \frac{e^{x_i}}{\sum_j e^{x_j}}$ | 输出概率分布 | 多分类输出 |

**直观理解**：

> - Sigmoid：把任何数"压缩"到0和1之间，适合做概率输出
> - ReLU：负数一律清零，正数保持不变——简单高效，是深度学习的默认选择
> - Softmax：把一组数变成概率分布，所有输出加起来等于1

---

## F. 符号速查表

| 符号 | 含义 | 首次出现章节 |
|------|------|------------|
| $s, S_t$ | 状态（state） | 第7章 |
| $a, A_t$ | 动作（action） | 第7章 |
| $r, R_t$ | 奖励（reward） | 第7章 |
| $\pi(a|s)$ | 策略（policy） | 第7章 |
| $V(s)$ / $v_\pi(s)$ | 状态值函数（state-value function） | 第7章 |
| $Q(s,a)$ / $q_\pi(s,a)$ | 动作值函数（action-value function） | 第7章 |
| $\gamma$ | 折扣因子（discount factor） | 第7章 |
| $\alpha$ | 学习率（learning rate） | 第7章 |
| $\varepsilon$ | 探索率（exploration rate） | 第7章 |
| $\delta_t$ | TD误差（TD error） | 第7章 |
| $\mathbf{w}, \boldsymbol{\theta}$ | 参数向量（parameters） | 第8章 |
| $\nabla$ | 梯度算子（gradient） | 第8章 |
| $\hat{v}(s, \mathbf{w})$ | 近似值函数（approximate value function） | 第8章 |
| $\nabla_{\boldsymbol{\theta}} \ln \pi$ | 得分函数 / 对数策略梯度 | 第9章 |
| $J(\boldsymbol{\theta})$ | 策略目标函数 | 第9章 |
| $A(s, a)$ | 优势函数（advantage function） | 第10章 |
| $\mathcal{T}$ | 贝尔曼算子（Bellman operator） | 附录 |

---

## 总结

本附录覆盖了强化学习所需的四大数学基础：

1. **概率论**：理解状态转移、策略选择、期望回报等随机过程
2. **期望与方差**：理解值函数的定义、梯度估计的偏差-方差权衡
3. **梯度下降**：理解所有参数更新方法的基础
4. **收敛性**：理解算法为什么能工作、在什么条件下收敛

### 学习建议

```
如果你是数学基础薄弱的读者：
    1. 先读附录的概率和期望部分（A、B）
    2. 了解梯度下降的基本概念（C.1-C.3）
    3. 遇到具体算法时再回来查阅相关数学工具

如果你有较好的数学基础：
    1. 快速浏览本附录确认符号约定
    2. 重点阅读压缩映射定理（D.2）和收敛性总结（D.5）
    3. 主要精力放在第7-10章的算法上
```

### 一句话总结

> **概率论告诉我们什么是随机的，期望告诉我们结果大概是什么，梯度下降告诉我们如何优化，收敛性告诉我们优化会成功。**

---

*本附录内容参考了Sutton & Barto《Reinforcement Learning: An Introduction》第2版的数学基础部分，以及Boyd & Vandenberghe《Convex Optimization》中关于梯度下降的内容。*
