# 附录：数学预备知识

> 本附录只保留学习主线最常用的数学概念，适合作为阅读前速查或中途回看。

---

## A. 概率论基础（Probability Theory）

### A.1 随机变量（Random Variable）

- 大写字母通常表示随机变量，如 $X,S,A,R$。
- 小写字母通常表示具体取值，如 $x,s,a,r$。

白话解释：

随机变量不是“一个固定数字”，而是“试验结果还没发生前的变量”。

### A.2 概率（Probability）

概率记作：

$$p(x)=P(X=x)$$

基本性质：

$$0\le p(x)\le 1,\qquad \sum_x p(x)=1$$

在强化学习里最常见的两类概率是：

- $\pi(a|s)$：在状态 $s$ 下选动作 $a$ 的概率
- $p(s'|s,a)$：在状态 $s$ 执行动作 $a$ 后到达 $s'$ 的概率

### A.3 联合概率（Joint Probability）

$$p(x,y)=P(X=x,Y=y)$$

表示两个事件同时发生的概率。

### A.4 边缘概率（Marginal Probability）

$$p(x)=\sum_y p(x,y)$$

白话解释：

把另一个变量“加掉”，只保留自己关心的那部分概率。

### A.5 条件概率（Conditional Probability）

$$p(x|y)=\frac{p(x,y)}{p(y)}$$

表示“在已知 $Y=y$ 的前提下，$X=x$ 的概率”。

### A.6 全概率公式（Law of Total Probability）

$$p(x)=\sum_y p(x|y)p(y)$$

意思是：把所有可能情况分开算，再加总。

### A.7 贝叶斯公式（Bayes' Theorem）

$$p(x|y)=\frac{p(y|x)p(x)}{p(y)}$$

白话解释：

- 先验 $p(x)$：原本的判断
- 似然 $p(y|x)$：如果 $x$ 成立，看到数据 $y$ 的概率
- 后验 $p(x|y)$：看到数据后更新后的判断

### A.8 概率分布速查表

学习本书时，知道“分布是给随机变量赋概率的规则”就够用；常见分布如伯努利、二项、正态会在机器学习和深度学习里更常见。

---

## B. 期望（Expectation）

### B.1 期望的定义

离散情形：

$$\mathbb{E}[X]=\sum_x x\,p(x)$$

连续情形：

$$\mathbb{E}[X]=\int x\,p(x)\,dx$$

白话解释：

期望就是“按概率加权后的平均值”。

### B.2 函数的期望

$$\mathbb{E}[f(X)] = \sum_x f(x)p(x)$$

在强化学习里，值函数本质上就是某个“未来回报函数”的期望。

### B.3 期望的线性性

$$\mathbb{E}[aX+bY]=a\mathbb{E}[X]+b\mathbb{E}[Y]$$

这条性质在推导贝尔曼方程和梯度公式时非常常用。

### B.4 条件期望

$$\mathbb{E}[X|Y]$$

表示“在已知 $Y$ 的条件下，对 $X$ 的平均判断”。

### B.5 方差与标准差

方差常写成：

$$\mathrm{Var}(X)=\mathbb{E}\big[(X-\mathbb{E}[X])^2\big]$$

白话解释：

方差衡量的是随机变量波动有多大。

---

## C. 梯度下降预备知识

### C.1 梯度（Gradient）

梯度 $\nabla f(\theta)$ 表示函数增长最快的方向。

### C.2 梯度下降（Gradient Descent）

最基本更新式：

$$\theta_{k+1}=\theta_k-\alpha \nabla f(\theta_k)$$

白话解释：

- 梯度告诉你“往哪边变大最快”；
- 减去梯度就是“朝下降最快方向走”。

### C.3 随机梯度下降（Stochastic Gradient Descent, SGD）

当全量梯度难算时，用样本近似梯度：

$$\theta_{k+1}=\theta_k-\alpha \hat{\nabla}f(\theta_k)$$

### C.4 小批量梯度下降（Mini-batch Gradient Descent）

在深度学习里，最常见的是用一个小批量样本来估计梯度。

### C.5 强化学习中的梯度

策略梯度和 DQN 训练本质上都离不开梯度更新。

### C.6 链式法则（Chain Rule）

链式法则让神经网络中的误差可以从输出层一路传回前面参数。

---

## D. 序列收敛性（Convergence）

### D.1 收敛的定义

如果一个序列后面越来越接近某个固定值，就说它收敛。

### D.2 压缩映射定理（Contraction Mapping Theorem）

如果一个映射每次都会缩小距离，就存在唯一不动点，而且反复应用会收敛到它。

这正是值迭代等方法的重要理论基础。

### D.3 马尔可夫性质与收敛

马尔可夫性让很多递推关系可以只依赖当前状态，从而更容易建立稳定分析。

### D.4 随机逼近（Stochastic Approximation）

随机逼近研究带噪声的递推是否还能逐步接近真实解。

### D.5 强化学习中的收敛性总结

入门时要抓住三件事：

- 更新目标是什么；
- 步长如何设置；
- 为什么误差不会无限放大。

---

## E. 向量与矩阵基础

### E.1 向量和矩阵运算

在值函数矩阵形式和神经网络表示里，向量、矩阵乘法会频繁出现。

### E.2 激活函数

在深度强化学习中，神经网络常使用非线性激活函数提升表达能力。

---

## F. 符号速查表

- $s$：状态
- $a$：动作
- $r$：奖励
- $\pi(a|s)$：策略
- $p(s'|s,a)$：状态转移概率
- $G_t$：回报
- $v_\pi(s)$：状态值
- $q_\pi(s,a)$：动作值
- $\gamma$：折扣因子
- $\alpha$：学习率或步长

---

## 总结

### 学习建议

- 不需要先把所有数学都学完再读强化学习。
- 但遇到公式时，最好能立刻分清：这是概率、期望、梯度，还是递推关系。

### 一句话总结

> 这份附录的作用不是替代数学教材，而是帮你在阅读强化学习时不被最常见的数学符号卡住。
