# 第9章：策略梯度方法（Policy Gradient Methods）

## 9.0 为什么要直接学习策略？

### 9.0.1 值函数方法的局限

值函数方法虽然强大，但在以下场景里可能不够自然：

- 动作空间连续；
- 最优策略需要随机性；
- 我们更关心直接优化策略参数。

### 9.0.2 核心思想

不再先学“哪个动作值大”，而是直接学习参数化策略：

$$\pi_\theta(a|s)$$

目标是让策略的期望回报最大。

---

## 9.1 策略表示：从表格到函数

### 9.1.1 策略的数学表示

参数化策略写作：

$$\pi_\theta(a|s)$$

这里 $\theta$ 是策略参数。

### 9.1.2 神经网络表示策略

在深度强化学习里，神经网络常直接输出动作概率分布或动作参数。

---

## 9.2 策略梯度定理（Policy Gradient Theorem）

### 9.2.1 目标函数

一般把目标写成：

$$J(\theta) = \mathbb{E}_{\pi_\theta}[G_0]$$

白话解释：

- $J(\theta)$ 表示当前策略参数下，平均能拿到多少长期收益；
- 学习就是想办法把它调大。

### 9.2.2 策略梯度定理

核心形式可写成：

$$\nabla_\theta J(\theta) \propto \mathbb{E}_{\pi_\theta}\big[\nabla_\theta \log \pi_\theta(a|s)\, q_{\pi_\theta}(s,a)\big]$$

白话解释：

- 如果某个动作带来了更高回报，就提高它在该状态下被选中的概率；
- 如果某类动作效果差，就降低它们的概率。

### 9.2.3 得分函数（Score Function）

$$\nabla_\theta \log \pi_\theta(a|s)$$

这一项描述的是：

> 参数变化时，当前动作概率会往哪个方向变化、变化多大。

### 9.2.4 从Q值到实际回报

在实际算法中，常常用采样得到的回报近似动作值，从而得到可计算的梯度估计。

---

## 9.3 REINFORCE算法：蒙特卡洛策略梯度

### 9.3.1 算法核心

REINFORCE 用完整回合样本来估计策略梯度。

### 9.3.2 梯度估计

典型更新形式：

$$\theta \leftarrow \theta + \alpha \, G_t \, \nabla_\theta \log \pi_\theta(A_t|S_t)$$

白话解释：

- 如果某一步动作最终带来了较大回报；
- 就增加今后在类似状态下选这个动作的概率。

### 9.3.3 REINFORCE算法伪代码

```text
初始化策略参数 θ
重复：
    用当前策略生成完整回合
    对回合中每个时刻 t：
        计算从 t 开始的回报 Gt
        θ ← θ + α Gt ∇θ log πθ(At|St)
```

### 9.3.4 直观理解

REINFORCE 本质上是在做：

- 成功动作多鼓励；
- 失败动作少鼓励。

### 9.3.5 REINFORCE的问题

主要问题是方差较大，因为它直接依赖完整回合回报。

---

## 9.4 基线（Baseline）：减小方差

### 9.4.1 核心思想

在不改变期望梯度方向的前提下，减掉一个基线可以降低方差。

### 9.4.2 为什么基线有效？

常见更新变成：

$$\theta \leftarrow \theta + \alpha \, \big(G_t - b(S_t)\big)\, \nabla_\theta \log \pi_\theta(A_t|S_t)$$

白话解释：

- 不是只看这次回报高不高；
- 而是看“它比通常水平高多少”。

### 9.4.3 常用的基线选择

最常见的是状态值函数 $V(s)$。

### 9.4.4 带基线的REINFORCE算法伪代码

```text
生成完整回合
对每个时刻 t：
    计算 Gt
    计算基线 b(St)
    用 (Gt - b(St)) 更新策略参数
```

### 9.4.5 优势函数（Advantage Function）

优势函数常定义为：

$$A_\pi(s,a) = q_\pi(s,a) - v_\pi(s)$$

白话解释：

- 动作值告诉你“做这个动作整体值多少”；
- 状态值告诉你“在这个状态通常值多少”；
- 两者相减后，就得到“这个动作比平均水平好多少”。

---

## 9.5 策略梯度的形式化推导（简化版）

### 9.5.1 从目标函数到梯度

目标是最大化 $J(\theta)$，因此关键在于找到它的梯度方向。

### 9.5.2 策略梯度定理的推导思路

入门时不必拘泥每一步细节，更重要的是明白推导的逻辑：

- 目标是期望回报；
- 期望依赖策略；
- 策略依赖参数；
- 所以可以通过对对数概率求梯度，把“提高好动作概率”写成公式。

### 9.5.3 策略梯度的蒙特卡洛估计

REINFORCE 就是这种思想最直接的采样实现。

---

## 9.6 算法对比总结

- 值函数方法：先学“值”，再间接得到策略。
- 策略梯度方法：直接学策略参数。
- 带基线的策略梯度：在保持方向正确的同时降低方差。

---

## 9.7 本章总结

### 核心要点

- 策略梯度直接优化参数化策略。
- REINFORCE 是最基本的蒙特卡洛策略梯度方法。
- 基线和优势函数用于降低方差、提高训练稳定性。

### 一句话总结

> 策略梯度方法的核心，是让高回报动作在未来更容易再次被选中。
