# 第9章 策略梯度方法（Policy Gradient Methods）

> **本章核心**：之前的方法都是先学值函数再导出策略（值函数方法）。本章换个思路——**直接学习策略**，用参数化函数表示策略，通过梯度上升来优化策略。这种方法在连续动作空间和随机策略场景中尤为强大。

---

## 9.0 为什么要直接学习策略？

### 9.0.1 值函数方法的局限

在前面几章中，我们学习的是**值函数**（$V(s)$ 或 $Q(s,a)$），策略通过值函数间接得到（如 $\varepsilon$-贪婪）。这种方法有一些局限：

| 问题 | 说明 | 例子 |
|------|------|------|
| **连续动作空间** | $Q$值方法需要取 $\max_a$，连续空间无法穷举 | 机器人关节角度控制 |
| **随机策略** | 某些问题最优策略是随机的（如扑克中的诈唬） | 石头剪刀布的最优策略是均匀随机 |
| **状态表示复杂** | 有时策略比值函数更简单 | 自动驾驶中"靠右行驶"可以直接学 |

### 9.0.2 核心思想

> 与其学习"状态值多少"，不如直接学习"在每种状态下该做什么"。

**参数化策略（Parameterized Policy）**：

$$\pi(a|s, \boldsymbol{\theta})$$

表示在状态 $s$ 下，参数为 $\boldsymbol{\theta}$ 时选择动作 $a$ 的概率。

**目标**：找到最优参数 $\boldsymbol{\theta}^*$，使得期望回报最大化：

$$J(\boldsymbol{\theta}) = \mathbb{E}_{\pi_\theta}[G_0] \quad \rightarrow \quad \max_{\boldsymbol{\theta}}$$

---

## 9.1 策略表示：从表格到函数

### 9.1.1 策略的数学表示

**离散动作空间**（如游戏按键）：

$$\pi(a|s, \boldsymbol{\theta}) = \frac{e^{h(s,a,\boldsymbol{\theta})}}{\sum_{b} e^{h(s,b,\boldsymbol{\theta})}}$$

这是**Softmax策略**——神经网络的输出经过Softmax函数，变成概率分布。$h(s,a,\boldsymbol{\theta})$ 是参数化的**打分函数（Score Function）**。

**连续动作空间**（如机器人控制）：

$$\pi(a|s, \boldsymbol{\theta}) = \mathcal{N}\big(\mu(s, \boldsymbol{\theta}), \sigma^2(s, \boldsymbol{\theta})\big)$$

用**正态分布（高斯分布）**表示策略——神经网络输出均值和标准差，动作从这个分布中采样。

### 9.1.2 神经网络表示策略

```
神经网络策略（Neural Network Policy）：

输入层          隐藏层           输出层
┌─────┐    ┌──────────┐    ┌──────────┐
│ s₁  │───▶│          │───▶│ π(a₁|s)  │
│ s₂  │───▶│   多层    │───▶│ π(a₂|s)  │
│ s₃  │───▶│   神经网络│───▶│   ...    │
│ ... │    │   (ReLU)  │    │ π(aₘ|s)  │
│ sₙ  │    │          │    └──────────┘
└─────┘    └──────────┘         ↑
                                Softmax

输入：状态 s（如游戏画面像素）
输出：每个动作的概率 π(a|s, θ)
```

**直观理解**：

> 神经网络就像一个**决策顾问**。你告诉它当前情况（状态），它分析后告诉你每个可选方案的好坏概率。不同于值函数方法（先算值再选动作），策略梯度直接让网络输出"该做什么"。

---

## 9.2 策略梯度定理（Policy Gradient Theorem）

### 9.2.1 目标函数

策略梯度的目标是最大化期望累积奖励：

$$J(\boldsymbol{\theta}) = \sum_{s} \mu_{\pi}(s) \sum_{a} \pi(a|s, \boldsymbol{\theta}) q_{\pi}(s,a)$$

其中：
- $\mu_{\pi}(s)$：在策略 $\pi$ 下访问状态 $s$ 的概率
- $\pi(a|s, \boldsymbol{\theta})$：选择动作 $a$ 的概率
- $q_{\pi}(s,a)$：动作值函数

### 9.2.2 策略梯度定理

**策略梯度定理**给出了目标函数梯度的简洁表达：

$$\nabla_{\boldsymbol{\theta}} J(\boldsymbol{\theta}) \propto \sum_{s} \mu(s) \sum_{a} q_{\pi}(s,a) \nabla_{\boldsymbol{\theta}} \pi(a|s, \boldsymbol{\theta})$$

**等价形式**（更常用的写法）：

$$\nabla_{\boldsymbol{\theta}} J(\boldsymbol{\theta}) = \mathbb{E}_{\pi} \Big[ \underbrace{q_{\pi}(S_t, A_t)}_{\text{动作值}} \cdot \underbrace{\nabla_{\boldsymbol{\theta}} \ln \pi(A_t|S_t, \boldsymbol{\theta})}_{\text{对数策略梯度}} \Big]$$

### 9.2.3 得分函数（Score Function）

$$\nabla_{\boldsymbol{\theta}} \ln \pi(a|s, \boldsymbol{\theta}) = \frac{\nabla_{\boldsymbol{\theta}} \pi(a|s, \boldsymbol{\theta})}{\pi(a|s, \boldsymbol{\theta})}$$

**为什么叫得分函数？** 它衡量了策略参数的变化如何影响动作选择的概率。如果某个动作的得分函数大，说明稍微调整参数就会显著改变选这个动作的概率。

**直观理解**：

> 得分函数就像一个**灵敏度指示器**。它告诉你："如果你稍微调整一下参数，选这个动作的概率会怎样变化？"如果得分函数为正，说明增加参数会使这个动作更可能被选中。

### 9.2.4 从Q值到实际回报

由于我们不知道真实的 $q_{\pi}(s,a)$，可以用实际回报 $G_t$ 来代替：

$$\nabla_{\boldsymbol{\theta}} J(\boldsymbol{\theta}) \approx \mathbb{E}_{\pi} \Big[ G_t \cdot \nabla_{\boldsymbol{\theta}} \ln \pi(A_t|S_t, \boldsymbol{\theta}) \Big]$$

这就是**蒙特卡洛策略梯度**的基础——用一次实际的回合回报 $G_t$ 来估计 $q_{\pi}(S_t, A_t)$。

---

## 9.3 REINFORCE算法：蒙特卡洛策略梯度

### 9.3.1 算法核心

REINFORCE是最基础的策略梯度算法，它用**完整的回合回报** $G_t$ 来估计梯度。

### 9.3.2 梯度估计

$$\boldsymbol{\theta} \leftarrow \boldsymbol{\theta} + \alpha \cdot G_t \cdot \nabla_{\boldsymbol{\theta}} \ln \pi(A_t|S_t, \boldsymbol{\theta})$$

### 9.3.3 REINFORCE算法伪代码

```
算法：REINFORCE（蒙特卡洛策略梯度）
输入：学习率 α，折扣因子 γ
初始化：策略参数 θ（随机初始化）

对每一个回合：
    // 1. 生成完整回合
    根据策略 π(·|·,θ) 生成一条轨迹：
        S₀, A₀, R₁, S₁, A₁, R₂, ..., S_{T-1}, A_{T-1}, R_T
    
    // 2. 计算每个时刻的回报
    对 t = 0, 1, ..., T-1：
        G_t = R_{t+1} + γ·R_{t+2} + γ²·R_{t+3} + ... + γ^{T-t-1}·R_T
    
    // 3. 梯度上升更新参数
    对 t = 0, 1, ..., T-1：
        θ ← θ + α · G_t · ∇_θ ln π(A_t|S_t, θ)
```

### 9.3.4 直观理解

REINFORCE的核心逻辑非常直觉：

> **如果某个动作带来了高回报，就增加选这个动作的概率；如果带来了低回报，就减少选这个动作的概率。**

**类比：厨师改进菜谱**

想象一个厨师（策略）在做菜：
1. 他做了一系列操作（一个回合）：先切菜，再调味，再翻炒...
2. 最后客人评分 $G_t$（回报）
3. 如果评分很高，他就**强化**那些做过的操作——以后做这些操作的概率增加
4. 如果评分很低，他就**弱化**那些操作——以后减少做这些操作

$G_t$ 就是"放大器"——回报越大，这次经验对参数的影响越大。

### 9.3.5 REINFORCE的问题

| 问题 | 说明 |
|------|------|
| **高方差** | $G_t$ 的方差很大，导致梯度估计噪声大，训练不稳定 |
| **需要完整回合** | 必须等回合结束才能更新（纯MC方法） |
| **收敛慢** | 高方差导致学习效率低 |

---

## 9.4 基线（Baseline）：减小方差

### 9.4.1 核心思想

REINFORCE的问题在于方差太大。一个技巧是引入**基线（Baseline）** $b(S_t)$：

$$\boldsymbol{\theta} \leftarrow \boldsymbol{\theta} + \alpha \cdot \underbrace{(G_t - b(S_t))}_{\text{优势信号}} \cdot \nabla_{\boldsymbol{\theta}} \ln \pi(A_t|S_t, \boldsymbol{\theta})$$

### 9.4.2 为什么基线有效？

数学上可以证明，减去基线**不改变梯度的期望值**（无偏），但可以**减小方差**。

$$\mathbb{E}_{\pi} \Big[ (G_t - b(S_t)) \nabla \ln \pi \Big] = \mathbb{E}_{\pi} \Big[ G_t \nabla \ln \pi \Big]$$

**直观理解**：

> 想象你在评估一场比赛的表现。如果一个选手得了8分：
> - 不加基线："他得了8分，不错" → 强化他的所有动作
> - 加基线（平均分=7分）："他只比平均分高1分" → 只适度强化
>
> 这样，只有**超出平均水平**的动作才被强化，普通表现的动作被忽略。

### 9.4.3 常用的基线选择

| 基线 $b(S_t)$ | 说明 |
|-------------|------|
| 常数（如 $b = \mathbb{E}[G_t]$） | 最简单，对所有状态都一样 |
| 状态值函数 $v_{\pi}(S_t)$ | 最常用，表示"从状态 $S_t$ 开始的平均回报" |
| 指数移动平均 | 在线估计平均回报，随训练变化 |

### 9.4.4 带基线的REINFORCE算法伪代码

```
算法：REINFORCE with Baseline（带基线的策略梯度）
输入：学习率 α^θ（策略），α^w（值函数），折扣因子 γ
初始化：策略参数 θ，值函数参数 w

对每一个回合：
    // 1. 生成完整回合
    根据策略 π(·|·,θ) 生成一条轨迹：
        S₀, A₀, R₁, S₁, A₁, R₂, ..., S_{T-1}, A_{T-1}, R_T
    
    // 2. 计算每个时刻的回报
    对 t = 0, 1, ..., T-1：
        G_t = R_{t+1} + γ·R_{t+2} + ... + γ^{T-t-1}·R_T
    
    // 3. 更新值函数（学习基线）
    对 t = 0, 1, ..., T-1：
        δ_t = G_t - v̂(S_t, w)           // 预测误差
        w ← w + α^w · δ_t · ∇_w v̂(S_t, w)  // 更新值函数
    
    // 4. 带基线的策略梯度更新
    对 t = 0, 1, ..., T-1：
        θ ← θ + α^θ · (G_t - v̂(S_t, w)) · ∇_θ ln π(A_t|S_t, θ)
```

### 9.4.5 优势函数（Advantage Function）

当我们用 $v_{\pi}(S_t)$ 作为基线时，$G_t - v_{\pi}(S_t)$ 实际上就是**优势（Advantage）**的蒙特卡洛估计：

$$A(s, a) = q_{\pi}(s, a) - v_{\pi}(s)$$

**优势的含义**：

> 动作 $a$ 比"平均水平"好多少？
> - $A(s,a) > 0$：这个动作比平均好 → 增加其概率
> - $A(s,a) < 0$：这个动作比平均差 → 减少其概率
> - $A(s,a) = 0$：这个动作很普通 → 不做调整

---

## 9.5 策略梯度的形式化推导（简化版）

### 9.5.1 从目标函数到梯度

$$J(\boldsymbol{\theta}) = \sum_{s} \mu_{\pi}(s) \sum_{a} \pi(a|s, \boldsymbol{\theta}) q_{\pi}(s,a)$$

### 9.5.2 策略梯度定理的推导思路

关键步骤：

$$\nabla_{\boldsymbol{\theta}} \pi(a|s, \boldsymbol{\theta}) = \pi(a|s, \boldsymbol{\theta}) \cdot \nabla_{\boldsymbol{\theta}} \ln \pi(a|s, \boldsymbol{\theta})$$

这个等式来自**对数导数技巧（Log-Derivative Trick）**：

$$\nabla \ln f(x) = \frac{\nabla f(x)}{f(x)} \quad \Rightarrow \quad \nabla f(x) = f(x) \nabla \ln f(x)$$

### 9.5.3 策略梯度的蒙特卡洛估计

从期望变为采样：

$$\nabla_{\boldsymbol{\theta}} J(\boldsymbol{\theta}) \approx \frac{1}{N} \sum_{i=1}^{N} \sum_{t=0}^{T_i} G_t^{(i)} \nabla_{\boldsymbol{\theta}} \ln \pi(A_t^{(i)}|S_t^{(i)}, \boldsymbol{\theta})$$

---

## 9.6 算法对比总结

| 算法 | 梯度估计 | 方差 | 偏差 | 是否需要完整回合 |
|------|---------|------|------|----------------|
| REINFORCE | $G_t \nabla \ln \pi$ | 高 | 无 | ✅ 是 |
| REINFORCE + 常数基线 | $(G_t - b) \nabla \ln \pi$ | 中 | 无 | ✅ 是 |
| REINFORCE + 值基线 | $(G_t - v(S_t)) \nabla \ln \pi$ | 低 | 无 | ✅ 是 |
| Actor-Critic（下一章） | $\hat{A} \nabla \ln \pi$ | 低 | 有 | ❌ 否 |

---

## 9.7 本章总结

### 核心要点

1. **策略梯度 vs 值函数方法**：
   - 值函数方法：先学值函数 → 间接得到策略
   - 策略梯度：直接学策略 → 不需要值函数

2. **参数化策略**：$\pi(a|s, \boldsymbol{\theta})$ 用神经网络输出动作概率分布。

3. **策略梯度定理**：

$$\nabla J(\boldsymbol{\theta}) = \mathbb{E}_{\pi}\big[ q_{\pi}(s,a) \nabla \ln \pi(a|s, \boldsymbol{\theta}) \big]$$

4. **REINFORCE**：用实际回合回报 $G_t$ 估计梯度，每步按 $\boldsymbol{\theta} \leftarrow \boldsymbol{\theta} + \alpha G_t \nabla \ln \pi$ 更新。

5. **基线**：减去基线 $b(S_t)$ 可减小方差而不引入偏差，常用状态值函数 $v(S_t)$ 作为基线。

6. **优势函数**：$A(s,a) = q(s,a) - v(s)$，表示动作相对于平均水平的好坏。

### 一句话总结

> **策略梯度 = 用神经网络直接输出"做什么"，通过试错学习——好结果的动作被强化，坏结果的动作被弱化；减去基线让学习更稳定高效。**

---

*本章翻译和解释参考了Sutton & Barto《Reinforcement Learning: An Introduction》第2版第13章以及Sutton et al. (2000) 策略梯度定理论文。*
