# 第3章：最优状态值与贝尔曼最优方程（Optimal State Values and Bellman Optimality Equation）

## 学习目标

- 理解最优值函数和最优策略的定义。
- 掌握贝尔曼最优方程的含义。
- 知道为什么最优方程和普通贝尔曼方程不同。
- 了解收缩映射在收敛分析里的作用。

---

## 核心概念

第2章研究的是“给定策略，如何评估它”；本章研究的是：

> 在所有可能策略中，最好的那一个如何描述？

---

## 3.1 动机示例：如何改进策略？

### 从策略评估到策略改进

如果我们已经知道某策略下的动作值 $q_\pi(s,a)$，就可以比较同一状态下不同动作的好坏。

核心想法：

- 如果当前策略选的动作不是该状态下最值钱的动作；
- 那么策略仍然有改进空间。

### 例子

在状态 $s$ 下，如果

$$q_\pi(s,a_2) > q_\pi(s,a_1)$$

那就说明先选 $a_2$ 比先选 $a_1$ 更好。

学习提示：

- “评估”回答的是当前策略怎样。
- “改进”回答的是还能不能做得更好。

---

## 3.2 最优状态值与最优策略

### 最优状态值的定义

$$v^*(s) = \max_\pi v_\pi(s)$$

白话解释：

- 在状态 $s$ 下，把所有策略都拿来比较；
- 能得到的最大长期期望回报，就是最优状态值。

### 最优策略的定义

$$v_{\pi^*}(s) = v^*(s), \quad \forall s \in \mathcal{S}$$

意思是：策略 $\pi^*$ 在每个状态上都达到最优值。

### 最优策略的存在性

- 在有限 MDP 里，最优策略至少存在一个。
- 最优策略可能不唯一。
- 但最优状态值函数通常是唯一的。

### 生活类比：最优路线

你可以把最优策略理解为“在每个路口都做出最合适选择的行路规则”。

---

## 贝尔曼最优方程（Bellman Optimality Equation）

### 核心方程

$$v^*(s) = \max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v^*(s')\right]$$

### 直观理解

这个公式和第2章最大的差别在于：

- 普通贝尔曼方程对动作做的是“按策略加权平均”；
- 贝尔曼最优方程对动作做的是“直接取最优动作”。

一句白话：

> 当前状态的最优值，就是先挑一个最好的动作，再把“当前奖励 + 未来最优值”算出来。

### 生活类比：在每个路口做最优选择

如果你已经知道每条后续路线的最好可能结果，那么在当前路口只需要选那个能把总收益做大的方向。

---

## 最优动作值（Optimal Action Value）

### 定义

$$q^*(s,a) = \mathbb{E}\left[G_t \mid S_t=s, A_t=a, \text{之后最优地行动}\right]$$

它表示：先做动作 $a$，之后都按最优方式行动时，平均能得到多少回报。

### 贝尔曼最优方程的另一种写法

状态值和动作值之间有关系：

$$v^*(s) = \max_a q^*(s,a)$$

也可以把最优动作值写成递归式：

$$q^*(s,a) = \sum_{s'} p(s'|s,a)\left[r + \gamma \max_{a'} q^*(s',a')\right]$$

白话解释：

- 先固定第一步动作；
- 到下一个状态后，再选最好的后续动作。

---

## 贝尔曼最优方程的特性

### 特性一：非线性方程

因为有 $\max$，所以它不再是线性方程组。

### 特性二：有唯一解

虽然是非线性的，但在常见设定下，它仍然有唯一最优值函数解。

### 特性三：解出 $v^*$ 即可得到最优策略

一旦知道 $v^*(s)$，就可以通过贪婪选择恢复最优策略。

---

## 3.3 收缩映射定理（Contraction Mapping Theorem）

### 为什么需要这个定理？

它回答一个关键问题：

> 为什么反复更新值函数，最后真的会收敛到最优值？

### 压缩映射的定义

如果一个算子每次都会把两个函数之间的距离缩小，就可以叫收缩映射。

### 收缩映射定理（Banach不动点定理）

核心结论：

- 收缩映射有唯一不动点；
- 反复迭代会收敛到这个不动点。

### 应用于贝尔曼最优方程

贝尔曼最优算子：

$$ (T^*v)(s) = \max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v(s')\right] $$

在常见条件下是一个 $\gamma$-收缩映射，因此不断应用它会收敛到唯一的不动点，也就是 $v^*$。

### 生活类比：收缩映射就像相机的自动对焦

每调整一次，都比之前更接近真正焦点；调整足够多次后，就会稳定在清晰位置。

---

## 3.4 从最优状态值到最优策略

### 核心流程

1. 先求出最优值函数 $v^*$ 或最优动作值函数 $q^*$。
2. 再在每个状态下选使右侧最大的动作。

### 贪婪策略（Greedy Policy）

基于 $v^*$ 的贪婪策略可写成：

$$\pi^*(s) \in \arg\max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v^*(s')\right]$$

白话解释：

- 看当前每个动作能带来的“即时奖励 + 后续最优价值”；
- 选最大的那个动作。

### 直观理解

最优值函数告诉你“每个状态的最好前景”，贪婪策略则告诉你“为了实现这个前景，现在该怎么走”。

---

## 本章总结

### 知识脉络图

- 状态值用于评估。
- 最优值用于描述理论上最好的可能结果。
- 贝尔曼最优方程把“最优”写进递归关系。
- 贪婪策略把最优值转成具体动作选择。

### 关键公式速查

最优状态值：

$$v^*(s) = \max_\pi v_\pi(s)$$

贝尔曼最优方程：

$$v^*(s) = \max_a \sum_{s'} p(s'|s,a)\left[r + \gamma v^*(s')\right]$$

最优动作值：

$$q^*(s,a) = \sum_{s'} p(s'|s,a)\left[r + \gamma \max_{a'} q^*(s',a')\right]$$

### 普通贝尔曼方程 vs 贝尔曼最优方程

- 普通方程：给定策略下做期望。
- 最优方程：在动作上直接取最大值。

## 关键概念回顾

- 最优值函数：每个状态下理论最好的长期收益。
- 最优策略：达到最优值函数的策略。
- 收缩映射：保证迭代收敛的重要数学工具。
