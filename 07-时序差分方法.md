# 第7章 时序差分方法（Temporal-Difference Methods）

> 本章是强化学习主线中的关键章节。TD 方法的核心优势是：不需要模型，而且不必等到整个回合结束才能更新。

---

## 7.0 核心思想：TD究竟是什么？

### 一个生活类比：预测通勤时间

TD 的直觉可以这样理解：

- 蒙特卡洛：到公司以后，才根据今天总用时修正估计。
- TD：开到半路时，已经可以用“已花时间 + 剩余估计时间”来提前更新。

一句话概括：

> TD 用“一步真实奖励 + 对下一步的当前估计”来修正当前值。

---

## 7.1 最基本的TD算法：TD(0)

### 7.1.1 问题设定

目标：在无模型条件下估计给定策略 $\pi$ 的状态值函数。

### 7.1.2 TD(0)更新公式

$$V(S_t) \leftarrow V(S_t) + \alpha \big[ R_{t+1} + \gamma V(S_{t+1}) - V(S_t) \big]$$

白话解释：

- 当前估计是 $V(S_t)$；
- 新目标不是完整回报，而是“即时奖励 $R_{t+1}$ + 下一状态估计值 $\gamma V(S_{t+1})$”；
- 两者差值就是这一步发现的误差。

通常把

$$\delta_t = R_{t+1} + \gamma V(S_{t+1}) - V(S_t)$$

叫做 TD 误差。

它表示：

> 当前预测和“一步观察后得到的新预测”之间差了多少。

### 7.1.3 直观理解

TD(0) 是一种“边走边纠错”的方法。

- 发现当前状态被高估，就下调；
- 发现被低估，就上调。

### 7.1.4 TD(0)算法伪代码

```text
初始化 V(s)
对每个回合：
    重复直到终止：
        根据策略 π 行动
        观察奖励 R 和下一状态 S'
        V(S) ← V(S) + α [R + γV(S') - V(S)]
        S ← S'
```

### 7.1.5 MC vs TD：关键区别

| 特性 | 蒙特卡洛 | TD |
|:---:|:---|:---|
| 更新时机 | 回合结束后 | 每一步都能更新 |
| 目标 | 实际完整回报 | 一步奖励 + 下一状态估计 |
| 是否自举 | 否 | 是 |
| 方差 | 通常较大 | 通常较低 |

学习提示：

- TD 的代价是引入偏差；
- 但它换来了更快、更稳定的在线更新能力。

---

## 7.2 Sarsa算法：同策略TD控制（On-Policy TD Control）

### 7.2.1 从状态值到动作值

控制问题需要知道“动作值”而不仅是“状态值”，因此 Sarsa 学习的是 $Q(s,a)$。

### 7.2.2 Sarsa更新公式

$$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha \big[ R_{t+1} + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t) \big]$$

白话解释：

- 先看当前动作值估计是多少；
- 再看执行后拿到奖励、并且下一步按当前策略实际会选什么动作；
- 用这个“真实执行链条上的下一步估计”来修正当前动作值。

### 7.2.3 Sarsa算法伪代码

```text
初始化 Q
对每个回合：
    根据 ε-贪婪选初始动作 A
    重复直到终止：
        执行动作 A，观察 R, S'
        在 S' 下按 ε-贪婪选择 A'
        Q(S,A) ← Q(S,A) + α [R + γQ(S',A') - Q(S,A)]
        S ← S', A ← A'
```

### 7.2.4 ε-贪婪策略说明

- 大多数时候选当前最好动作；
- 少数时候随机探索。

### 7.2.5 Sarsa的同策略特性

Sarsa 是 on-policy，因为：

- 它一边执行当前策略；
- 一边学习的也是这条当前策略的价值。

---

## 7.3 Q-Learning算法：异策略TD控制（Off-Policy TD Control）

### 7.3.1 最重要的强化学习算法之一

Q-Learning 的经典地位在于：形式简单、思想清晰、影响深远。

### 7.3.2 Q-Learning更新公式

$$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha \big[ R_{t+1} + \gamma \max_a Q(S_{t+1}, a) - Q(S_t, A_t) \big]$$

白话解释：

- Sarsa 用的是“下一步实际选到的动作值”；
- Q-Learning 用的是“下一状态下理论上最好的动作值”。

所以它更“理想化”，目标直接朝最优动作靠近。

### 7.3.3 与Sarsa的关键区别

Sarsa：

$$R_{t+1} + \gamma Q(S_{t+1}, A_{t+1})$$

Q-Learning：

$$R_{t+1} + \gamma \max_a Q(S_{t+1},a)$$

一句总结：

- Sarsa 学“当前行为策略的价值”；
- Q-Learning 学“最优策略的价值”。

### 7.3.4 Q-Learning算法伪代码

```text
初始化 Q
对每个回合：
    重复直到终止：
        按行为策略选择动作 A
        执行动作，观察 R, S'
        Q(S,A) ← Q(S,A) + α [R + γ max_a Q(S',a) - Q(S,A)]
        S ← S'
```

### 7.3.5 异策略的直观理解

可以一边“带探索地执行”，一边“按更贪婪、更理想的目标学习”。

---

## 7.4 Expected Sarsa：用期望代替采样

### 7.4.1 核心思想

Sarsa 依赖下一步采样动作 $A_{t+1}$，Expected Sarsa 则直接对下一步动作取期望。

### 7.4.2 Expected Sarsa更新公式

$$Q(S_t,A_t)\leftarrow Q(S_t,A_t)+\alpha\Big[R_{t+1}+\gamma \sum_{a}\pi(a|S_{t+1})Q(S_{t+1},a)-Q(S_t,A_t)\Big]$$

### 7.4.3 直观解释

- 不再只看“采样到的那一个动作”；
- 改成看“下一状态下整条策略的平均动作价值”。

这样通常能降低部分采样方差。

---

## 7.5 n步TD：在TD和MC之间折中

### 7.5.1 一个连续的谱系

- 1 步 TD：更新快，但更依赖估计；
- MC：等完整回合，目标真实但方差大；
- n 步 TD：介于两者之间。

### 7.5.2 n步回报（n-step Return）

$$G_t^{(n)} = R_{t+1} + \gamma R_{t+2} + \cdots + \gamma^{n-1}R_{t+n} + \gamma^n V(S_{t+n})$$

白话解释：

- 前面 $n$ 步用真实奖励；
- 第 $n$ 步之后，仍用一个估计值来接上。

### 7.5.3 n步TD更新

$$V(S_t)\leftarrow V(S_t)+\alpha \big[G_t^{(n)} - V(S_t)\big]$$

### 7.5.4 直观理解

n 越大：

- 越接近蒙特卡洛；
- 用到的真实信息越多；
- 但更新也越晚。

### 7.5.5 n步Sarsa

同样思路可以推广到动作值更新，形成 n 步 Sarsa。

---

## 7.6 算法对比总结

### Q-Learning vs Sarsa的经典对比

| 维度 | Sarsa | Q-Learning |
|:---:|:---|:---|
| 类型 | On-policy | Off-policy |
| 下一步目标 | 实际策略选出的动作 | 下一状态下最优动作 |
| 风格 | 更贴近真实执行策略 | 更直接逼近最优策略 |

---

## 7.7 本章总结

### 核心要点

- TD 的核心是“一步奖励 + 下一步估计”。
- Sarsa 学的是当前执行策略。
- Q-Learning 学的是最优动作值。
- n 步方法在 TD 和 MC 之间提供折中。

### 学习路径建议

建议顺序：

1. 先懂 TD(0)；
2. 再比较 Sarsa 和 Q-Learning；
3. 最后再看 Expected Sarsa 和 n 步方法。

### 一句话总结

> TD 方法让强化学习真正具备了“边交互边学习”的能力。
