# 第7章 时序差分方法（Temporal-Difference Methods）

> **本章核心**：时序差分（Temporal-Difference, TD）学习是强化学习中最重要、最核心的方法之一。它结合了**动态规划（Dynamic Programming, DP）**的"自举（bootstrapping）"思想和**蒙特卡洛（Monte Carlo, MC）**方法的"采样（sampling）"思想，是一种强大的无模型（model-free）学习方法。

---

## 7.0 核心思想：TD究竟是什么？

### 一个生活类比：预测通勤时间

想象你每天开车上班，想知道"从家出发到公司需要多久"。你有几种估算方式：

- **蒙特卡洛（MC）方法**：今天开完全程到公司后，记录实际用时50分钟。根据这次完整经验更新对"通勤时间"的估计。**必须等全程结束才能更新**。
- **时序差分（TD）方法**：开了一段路后，你看到导航显示"剩余路程还需20分钟"。你已经开了30分钟，于是立刻更新估计："这次通勤大约需要 30 + 20 = 50分钟"。**不用等全程结束，每走一步都能更新**。
- **动态规划（DP）方法**：你知道每条路的平均通行时间，用地图模型计算出理论最优路径时间。**你需要完整的道路模型**。

TD的精妙之处在于：**它像MC一样不需要环境模型，又像DP一样可以逐步更新**。每走一步，它就结合"实际观测到的奖励"和"对下一步的估计"来更新当前的估计。

---

## 7.1 最基本的TD算法：TD(0)

### 7.1.1 问题设定

- **目标**：估计给定策略 $\pi$ 下的状态值函数 $v_\pi(s)$
- **已知**：智能体与环境交互的样本序列（状态-奖励序列）
- **未知**：环境的转移概率和奖励函数（无模型设定）

### 7.1.2 TD(0)更新公式

$$V(S_t) \leftarrow V(S_t) + \alpha \big[ \underbrace{R_{t+1} + \gamma V(S_{t+1})}_{\text{TD目标}} - V(S_t) \big]$$

**各符号含义：**

| 符号 | 含义 | 直观解释 |
|------|------|---------|
| $V(S_t)$ | 当前对状态 $S_t$ 的价值估计 | 你现在认为这个状态值多少钱 |
| $R_{t+1}$ | 执行动作后获得的即时奖励 | 你实际拿到了多少奖励 |
| $\gamma$ | 折扣因子（$0 \leq \gamma \leq 1$） | 未来的钱不如现在的钱值钱 |
| $V(S_{t+1})$ | 下一状态的估计值 | 你对下一个状态的价值猜测 |
| $\alpha$ | 学习率（$0 < \alpha \leq 1$） | 新信息多大程度覆盖旧估计 |
| $R_{t+1} + \gamma V(S_{t+1})$ | **TD目标（TD Target）** | 你对回报的"新估计" |
| $\delta_t = R_{t+1} + \gamma V(S_{t+1}) - V(S_t)$ | **TD误差（TD Error）** | 新估计与旧估计的差距 |

### 7.1.3 直观理解

TD更新可以看作一个**纠错过程**：

> 你原来认为状态 $S_t$ 值 $V(S_t)$ 分。然后你走了这一步，获得了奖励 $R_{t+1}$，来到了新状态 $S_{t+1}$。你觉得 $S_{t+1}$ 大概值 $V(S_{t+1})$ 分。所以你估计走这一步的**总回报**应该是 $R_{t+1} + \gamma V(S_{t+1})$。如果这个新估计比你原来的估计高，说明你可能低估了 $S_t$ 的价值，就往上调一点；如果低了，就往下调。调整幅度由 $\alpha$ 控制。

### 7.1.4 TD(0)算法伪代码

```
算法：TD(0) 状态值估计
输入：策略 π，学习率 α，折扣因子 γ
初始化：对所有状态 s，V(s) = 0

对每一个回合：
    根据策略 π 选择初始状态 S
    对每一步（直到回合结束）：
        在状态 S 下根据策略 π 选择动作 A
        执行动作 A，观察即时奖励 R 和下一状态 S'
        
        // TD更新
        V(S) ← V(S) + α [R + γ·V(S') - V(S)]
        
        S ← S'
    直到 S 为终止状态
```

### 7.1.5 MC vs TD：关键区别

| 特性 | 蒙特卡洛（MC） | 时序差分（TD） |
|------|--------------|--------------|
| 更新时机 | 必须等回合结束 | **每步都可以更新** |
| 使用目标 | 实际回报 $G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1}$ | TD目标 $R_{t+1} + \gamma V(S_{t+1})$ |
| 是否自举 | 否（用实际完整回报） | **是**（用当前估计作为目标的一部分） |
| 是否需要完整回合 | 是 | 否 |
| 偏差（Bias） | 无偏 | 有偏（因自举引入偏差） |
| 方差（Variance） | 高 | 低 |

**关键洞察**：TD把"估计"拆成了"实际一步奖励"+"对下一步的估计"，因此方差更小（不用等全程的不确定性），但引入了偏差（因为用了自己的估计来估计自己，即自举）。

---

## 7.2 Sarsa算法：同策略TD控制（On-Policy TD Control）

### 7.2.1 从状态值到动作值

TD(0)估计的是状态值 $V(s)$，但我们要做控制（找到最优策略），需要知道每个**动作**的价值。因此Sarsa估计的是**动作值函数（Action-Value Function）** $Q(s,a)$。

### 7.2.2 Sarsa更新公式

$$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha \big[ R_{t+1} + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t) \big]$$

**为什么叫Sarsa？**

因为它的更新用到了五元组 $(S_t, A_t, R_{t+1}, S_{t+1}, A_{t+1})$：

> **S**tate → **A**ction → **R**eward → **S**tate → **A**ction

### 7.2.3 Sarsa算法伪代码

```
算法：Sarsa（同策略TD控制）
输入：学习率 α，折扣因子 γ，探索率 ε
初始化：对所有状态s和动作a，Q(s,a) = 0

对每一个回合：
    选择初始状态 S
    用 ε-贪婪策略根据 Q 选择动作 A
    
    对每一步（直到回合结束）：
        执行动作 A，观察即时奖励 R 和下一状态 S'
        用 ε-贪婪策略根据 Q 选择下一动作 A'
        
        // Sarsa更新
        Q(S,A) ← Q(S,A) + α [R + γ·Q(S',A') - Q(S,A)]
        
        S ← S'
        A ← A'
    直到 S 为终止状态
```

### 7.2.4 ε-贪婪策略说明

```
函数：ε-贪婪策略选择动作
输入：当前状态 S，动作值函数 Q，探索率 ε
输出：选择的动作 A

以概率 ε：          // 探索（Exploration）
    随机选择一个动作
以概率 1-ε：        // 利用（Exploitation）
    选择 Q(S,·) 最大的动作
```

**直观理解**：ε-贪婪策略就像一个人在做决策——大部分时间选择他认为最好的做法（利用），但偶尔也尝试一些新奇的方案（探索），以免错过更好的选择。

### 7.2.5 Sarsa的同策略特性

Sarsa是**同策略（On-Policy）**的，意味着：
- **行为策略（Behavior Policy）**：用来选择动作的策略（ε-贪婪策略）
- **目标策略（Target Policy）**：正在被学习的策略（也是ε-贪婪策略）
- **两者相同** → 同策略

---

## 7.3 Q-Learning算法：异策略TD控制（Off-Policy TD Control）

### 7.3.1 最重要的强化学习算法之一

Q-Learning由Watkins于1989年提出，是强化学习历史上最具影响力的算法之一。它是一个**异策略（Off-Policy）**算法。

### 7.3.2 Q-Learning更新公式

$$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha \big[ R_{t+1} + \gamma \underbrace{\max_{a'} Q(S_{t+1}, a')}_{\text{关键区别！}} - Q(S_t, A_t) \big]$$

### 7.3.3 与Sarsa的关键区别

| | Sarsa | Q-Learning |
|--|-------|------------|
| 目标中的下一步 | $Q(S', A')$ ——实际选择的下一动作 | $\max_{a'} Q(S', a')$ ——**最优**动作的Q值 |
| 策略类型 | 同策略（On-Policy） | **异策略（Off-Policy）** |
| 行为策略 | ε-贪婪 | ε-贪婪（或其他） |
| 目标策略 | ε-贪婪 | **贪婪策略**（始终选Q最大的动作） |

**核心区别**：Q-Learning在更新时假设下一步会采取**最优动作**（取max），无论实际上下一步做了什么。这使得Q-Learning学的是最优动作值函数 $Q^*$，而不受探索行为的影响。

### 7.3.4 Q-Learning算法伪代码

```
算法：Q-Learning（异策略TD控制）
输入：学习率 α，折扣因子 γ，探索率 ε
初始化：对所有状态s和动作a，Q(s,a) = 0

对每一个回合：
    选择初始状态 S
    
    对每一步（直到回合结束）：
        用 ε-贪婪策略根据 Q 选择动作 A
        执行动作 A，观察即时奖励 R 和下一状态 S'
        
        // Q-Learning更新（注意：用 max，不需要知道下一动作）
        Q(S,A) ← Q(S,A) + α [R + γ·max_a' Q(S',a') - Q(S,A)]
        
        S ← S'
    直到 S 为终止状态
```

### 7.3.5 异策略的直观理解

想象你在学习下棋：
- **Sarsa（同策略）**：你边下棋边学，学什么策略就用什么策略下。如果你经常下一些随机的棋（探索），你学到的也是这种"带点随机"的棋路。
- **Q-Learning（异策略）**：你可以用一些随意的、试验性的着法下棋（探索），但你学的是"如果每次都走最好的一步"会有多厉害。你的**学习**和**行动**是分离的。

---

## 7.4 Expected Sarsa：用期望代替采样

### 7.4.1 核心思想

Sarsa在计算TD目标时，对下一步的动作进行了一次**采样**（选择了一个具体的 $A'$）。Expected Sarsa则改为计算**期望值**——考虑所有可能的下一动作的加权平均。

### 7.4.2 Expected Sarsa更新公式

$$Q(S,A) \leftarrow Q(S,A) + \alpha \Big[ R + \gamma \underbrace{\sum_{a'} \pi(a'|S') Q(S',a')}_{\text{期望}~E[Q(S',\cdot)]} - Q(S,A) \Big]$$

### 7.4.3 直观解释

> Sarsa问："我下一步实际走的那个动作值多少钱？"
>
> Expected Sarsa问："考虑到我的策略，**平均来说**下一步值多少钱？"

Expected Sarsa消除了对 $A'$ 采样的随机性，因此方差比Sarsa更小，但每一步需要计算所有动作的期望，计算量稍大。

---

## 7.5 n步TD：在TD和MC之间折中

### 7.5.1 一个连续的谱系

TD学习和MC学习可以看作一个连续谱系的两个极端：

- **TD(0)**：只看1步奖励（1步自举）
- **n步TD**：看n步奖励，然后自举
- **MC（蒙特卡洛）**：看完整回合的所有奖励（∞步）

### 7.5.2 n步回报（n-step Return）

$$G_{t:t+n} = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \cdots + \gamma^{n-1} R_{t+n} + \gamma^n V(S_{t+n})$$

**展开写法：**

$$G_{t:t+n} = \sum_{i=1}^{n} \gamma^{i-1} R_{t+i} + \gamma^n V(S_{t+n})$$

**各项含义：**
- $\sum_{i=1}^{n} \gamma^{i-1} R_{t+i}$：接下来n步的实际奖励累积
- $\gamma^n V(S_{t+n})$：n步之后的自举估计

### 7.5.3 n步TD更新

$$V(S_t) \leftarrow V(S_t) + \alpha \big[ G_{t:t+n} - V(S_t) \big]$$

### 7.5.4 直观理解

想象你在预测一场足球比赛的结果：

- **TD(0)**：只看了一分钟的比分，就更新对最终结果的预测。
- **n步TD**：看了前n分钟的比分和场面，然后更新预测。
- **MC**：等到全场比赛结束，看到最终比分后才更新预测。

n越大，越依赖实际观测（方差大，偏差小）；n越小，越依赖自举估计（方差小，偏差大）。

### 7.5.5 n步Sarsa

对于动作值估计，n步Sarsa使用：

$$G_{t:t+n} = R_{t+1} + \gamma R_{t+2} + \cdots + \gamma^{n-1} R_{t+n} + \gamma^n Q(S_{t+n}, A_{t+n})$$

$$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha [G_{t:t+n} - Q(S_t, A_t)]$$

---

## 7.6 算法对比总结

| 算法 | 估计目标 | 策略类型 | TD目标 | 特点 |
|------|---------|---------|--------|------|
| TD(0) | $V(s)$ | — | $R + \gamma V(S')$ | 最基础的TD算法 |
| Sarsa | $Q(s,a)$ | 同策略 | $R + \gamma Q(S',A')$ | 保守，考虑实际探索 |
| Q-Learning | $Q^*(s,a)$ | 异策略 | $R + \gamma \max_{a'} Q(S',a')$ | 大胆，学习最优策略 |
| Expected Sarsa | $Q(s,a)$ | 同/异策略 | $R + \gamma \sum_{a'} \pi(a'|S')Q(S',a')$ | 方差更小 |
| n步TD | $V(s)$或$Q(s,a)$ | — | n步回报+自举 | 平衡偏差与方差 |

### Q-Learning vs Sarsa的经典对比

在一个有悬崖的环境中（Cliff Walking）：
- **Q-Learning**学到的路径沿着悬崖边缘走（最优但危险，因为它不考虑自己的探索行为）
- **Sarsa**学到的路径离悬崖远一点（次优但更安全，因为它考虑了自己有时会随机行动）

这说明：**Q-Learning更激进，追求最优；Sarsa更保守，考虑实际行为**。

---

## 7.7 本章总结

### 核心要点

1. **TD学习的本质**：用"即时奖励 + 下一步的估计值"来更新当前估计，兼具MC的采样优势和DP的自举优势。

2. **TD(0)**是最基础的TD算法，每步更新状态值估计。

3. **Sarsa**是同策略的TD控制算法，用五元组 $(S,A,R,S',A')$ 更新Q值，保守稳健。

4. **Q-Learning**是异策略的TD控制算法，用 $\max$ 操作更新Q值，直接学习最优策略，是强化学习中最经典的算法之一。

5. **Expected Sarsa**用期望代替采样，降低方差。

6. **n步TD**在TD(0)和MC之间提供灵活的权衡。

### 学习路径建议

```
TD(0) → Sarsa/Q-Learning → Expected Sarsa → n-step方法
         ↑
    先掌握这两个核心算法
```

### 一句话总结

> **TD学习 = 走一步，看一步奖励，猜一下未来，然后修正自己的判断。**

---

*本章翻译和解释参考了Sutton & Barto《Reinforcement Learning: An Introduction》第2版第6章。*
