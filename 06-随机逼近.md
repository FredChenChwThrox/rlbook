# 第6章：随机逼近（Stochastic Approximation）

## 6.1 动机示例：均值估计的增量形式

### 批量方法的困境

如果每次都重新收集全部样本再重算均值，代价太高，也不适合在线学习。

### 增量方法的出现

增量更新的一般形式是：

$$x_{k+1} = x_k + \alpha_k(\text{新信息})$$

它表达的是“在旧估计基础上，用新观测做小步修正”。

### 推广：任意步长

均值更新写成一般形式后，步长不必固定为 $\frac{1}{n}$，而可以用更一般的 $\alpha_k$。

---

## 6.2 随机逼近（Stochastic Approximation, SA）

### 问题设定

随机逼近研究的是：

> 当我们只能看到带噪声的观测时，如何逐步逼近某个真实解？

### Robbins-Monro 算法

经典递推形式：

$$x_{k+1}=x_k+\alpha_k\big(h(x_k)+M_{k+1}\big)$$

白话解释：

- $x_k$ 是当前估计；
- $h(x_k)$ 表示理论上该往哪个方向修正；
- $M_{k+1}$ 是噪声；
- $\alpha_k$ 控制这一步修正幅度。

### 更一般的形式

很多 RL 算法都能写成类似结构：旧估计 + 步长 × 误差项。

### 算法伪代码

```text
初始化参数 x
对每次新观测：
    计算误差或方向
    用步长 α 更新 x
```

---

## 6.3 收敛性条件

### 条件1：步长条件（最关键）

典型条件是：

$$\sum_k \alpha_k = \infty, \qquad \sum_k \alpha_k^2 < \infty$$

白话解释：

- 第一个条件保证“总共走得够远”，否则可能永远到不了目标附近；
- 第二个条件保证“后期抖动别太大”，否则噪声会一直干扰收敛。

### 步长示例

常见选择：

- $\alpha_k = \frac{1}{k}$
- $\alpha_k = \frac{1}{\sqrt{k}}$（不一定满足全部要求）

### 条件2：噪声条件

噪声不能无限失控，通常要求它的期望和方差有一定可控性。

### 条件3：函数条件

理论中的目标函数或漂移项通常也要满足一定连续性、稳定性条件。

### 条件4：步衰减速率

步长下降太快，可能学不到；下降太慢，可能一直抖动。

### 经典步长选择：$\alpha_k = \frac{1}{k}$

这是最典型也最容易分析的形式。

### 常数步长的特殊情况

常数步长更适合非平稳环境或深度学习实践，但理论上通常只能保证在解附近波动，而不是严格收敛到一点。

---

## 6.4 应用于强化学习

### 从SA到RL的桥梁

很多 RL 更新式本质上都是随机逼近。

### TD(0)作为随机逼近

TD(0) 更新可写成：

$$V(S_t)\leftarrow V(S_t)+\alpha\big(R_{t+1}+\gamma V(S_{t+1})-V(S_t)\big)$$

其中括号中的量就是带噪声的修正项。

### Q-Learning作为随机逼近

Q-Learning 也有同样结构：

$$Q(S_t,A_t)\leftarrow Q(S_t,A_t)+\alpha\big(R_{t+1}+\gamma \max_a Q(S_{t+1},a)-Q(S_t,A_t)\big)$$

### SA视角下的算法演进

从这个角度看，很多算法虽然名字不同，但共同骨架其实非常接近。

---

## 6.5 梯度下降作为随机逼近

### 优化问题的视角

如果目标是最小化损失函数 $L(\theta)$，梯度下降写成：

$$\theta_{k+1} = \theta_k - \alpha_k \nabla L(\theta_k)$$

### 梯度与求根的关系

最优点常常满足梯度为 0，因此梯度下降也可看成一种求根过程。

### 随机梯度下降（SGD）

SGD 用样本近似真实梯度：

$$\theta_{k+1} = \theta_k - \alpha_k \hat{\nabla} L(\theta_k)$$

白话解释：

- 真正梯度算不起时，用带噪声的小批量梯度代替；
- 这正是“随机逼近”的典型情形。

### SGD步长选择的启示

步长设计直接影响：

- 收敛速度
- 稳定性
- 是否容易震荡

### 深度强化学习中的SGD

后面 DQN、策略梯度、Actor-Critic 都会大量用到 SGD 思想。

---

## 直观理解：随机逼近的本质

### 核心思想一句话概括

> 在噪声环境里，不断用小步修正逼近真实解。

### 为什么步长必须趋于零？

- 如果步长永远很大，后期会一直来回抖动；
- 如果太早接近 0，又可能还没学到正确答案。

### 收敛的直观机制

前期允许大步探索，后期逐渐变小做精细调整。

---

## 本章总结

- 随机逼近是很多 RL 增量更新的理论基础。
- 关键结构是“旧估计 + 步长 × 误差”。
- 步长设计决定了学习是否稳定、是否收敛。

## 与其他章节的关系图

- 第5章讲“从样本学习”。
- 第6章讲“为什么这种增量更新可能成立”。
- 第7章开始把这种思想真正用于核心 RL 算法。
