# 第8章 值函数近似（Value Function Approximation）

> **本章核心**：当状态空间大到无法用表格存储所有值时，我们如何用**函数近似（Function Approximation）**来估计值函数？本章将从线性近似讲到深度神经网络（DQN），让强化学习能够处理复杂的高维问题。

---

## 8.0 核心问题：表格法的局限性

### 8.0.1 为什么需要函数近似？

在前面的章节中，我们用**表格（Tabular）**方法存储每个状态的值：

| 状态 | V(s) |
|------|------|
| s₁ | 3.5 |
| s₂ | 2.1 |
| s₃ | 7.8 |
| ... | ... |

**表格法的假设**：状态空间足够小，可以为每个状态分配一个存储位置。

**现实问题**：
- 围棋：棋盘状态数约为 $10^{170}$，远超宇宙中的原子数
- 自动驾驶：摄像头的像素输入，状态空间近乎无限
- 机器人控制：连续的传感器读数，状态不可数

### 8.0.2 核心思想

> 不再为每个状态单独存一个值，而是用一个**参数化函数**来拟合值函数。

$$\hat{v}(s, \mathbf{w}) \approx v_\pi(s)$$

$$\hat{q}(s, a, \mathbf{w}) \approx q_\pi(s, a)$$

其中 $\mathbf{w}$ 是可学习的参数向量。这样，我们只需要存储参数 $\mathbf{w}$，而不是每个状态的值。

**类比**：
- **表格法**像一本字典——每个词（状态）对应一个释义（值），词多了字典就厚得无法使用。
- **函数近似**像一个语言模型——它学习语言的规律，遇到新词也能推测出意思。

---

## 8.1 从表格到函数：表示方法的演进

### 8.1.1 统一视角

| 方法 | 表示形式 | 更新方式 | 适用场景 |
|------|---------|---------|---------|
| 表格法 | $V(s)$ 每个状态独立 | 直接赋值 | 小状态空间 |
| 线性近似 | $\hat{v}(s, \mathbf{w}) = \mathbf{w}^\top \boldsymbol{\phi}(s)$ | 梯度下降更新 $\mathbf{w}$ | 有良好特征 |
| 神经网络 | $\hat{v}(s, \mathbf{w}) = \text{NN}(s; \mathbf{w})$ | 反向传播更新 $\mathbf{w}$ | 复杂高维输入 |

### 8.1.2 特征向量（Feature Vector）

在线性近似中，状态 $s$ 被表示为一个**特征向量**：

$$\boldsymbol{\phi}(s) = \begin{bmatrix} \phi_1(s) \\ \phi_2(s) \\ \vdots \\ \phi_d(s) \end{bmatrix}$$

**例子**（棋盘游戏的状态特征）：
- $\phi_1(s)$ = 我方棋子数量
- $\phi_2(s)$ = 敌方棋子数量
- $\phi_3(s)$ = 我方国王是否安全
- $\phi_4(s)$ = 中心区域的控制力
- ...

> 特征是状态的人工表示，好的特征能让学习变得高效。深度学习的革命在于让神经网络**自动学习特征**。

---

## 8.2 线性函数近似（Linear Function Approximation）

### 8.2.1 基本形式

$$\hat{v}(s, \mathbf{w}) = \mathbf{w}^\top \boldsymbol{\phi}(s) = \sum_{i=1}^{d} w_i \phi_i(s)$$

**直觉理解**：

> 想象你在评估一套房子值多少钱。你会考虑几个因素：面积、地段、房龄、装修。每个因素有一个权重（面积更重要就权重大），最后把这些因素加权求和得到估价。线性函数近似就是这个思路——**特征的加权和**。

### 8.2.2 用梯度下降更新参数

**目标**：最小化预测值与目标值之间的误差

$$J(\mathbf{w}) = \mathbb{E}\Big[\big(U_t - \hat{v}(S_t, \mathbf{w})\big)^2\Big]$$

其中 $U_t$ 是目标（如TD目标 $R_{t+1} + \gamma \hat{v}(S_{t+1}, \mathbf{w})$）。

**梯度**：

$$\nabla_{\mathbf{w}} \hat{v}(S_t, \mathbf{w}) = \boldsymbol{\phi}(S_t)$$

**梯度下降更新**：

$$\mathbf{w} \leftarrow \mathbf{w} + \alpha \big[U_t - \hat{v}(S_t, \mathbf{w})\big] \boldsymbol{\phi}(S_t)$$

**各部分的含义**：
- $\alpha$：学习率
- $U_t - \hat{v}(S_t, \mathbf{w})$：预测误差
- $\boldsymbol{\phi}(S_t)$：当前状态的特征向量

### 8.2.3 半梯度TD(0)算法

```
算法：半梯度TD(0)（线性函数近似）
输入：策略 π，学习率 α，折扣因子 γ，特征映射 φ
初始化：参数向量 w = 0

对每一个回合：
    选择初始状态 S
    对每一步（直到回合结束）：
        在状态 S 下根据策略 π 选择动作 A
        执行动作 A，观察即时奖励 R 和下一状态 S'
        
        // 计算TD目标（注意：使用当前估计）
        δ ← R + γ·w^T φ(S') - w^T φ(S)
        
        // 更新参数
        w ← w + α·δ·φ(S)
        
        S ← S'
    直到 S 为终止状态
```

> **为什么叫"半梯度"？** 因为目标 $U_t = R + \gamma \hat{v}(S', \mathbf{w})$ 本身也依赖于 $\mathbf{w}$，但我们计算梯度时忽略了这部分（把它当作常数），只对了 $\hat{v}(S_t, \mathbf{w})$ 求梯度。

---

## 8.3 深度Q网络（Deep Q-Network, DQN）

### 8.3.1 为什么需要神经网络？

线性近似的局限在于需要**手工设计特征**。对于复杂问题（如从原始像素学习玩游戏），我们不知道什么特征是好的。

**神经网络**的优势：
- 直接从原始输入学习（如像素画面）
- 自动学习有用的特征表示
- 可以表示非线性函数关系

### 8.3.2 DQN的核心思想

DQN用**深度神经网络**来近似Q函数：

$$\hat{q}(s, a, \mathbf{w}) \approx q^*(s, a)$$

对于离散动作空间（如游戏手柄的按键），DQN通常输出每个动作的Q值：

$$\text{NN}(s; \mathbf{w}) = \begin{bmatrix} \hat{q}(s, a_1; \mathbf{w}) \\ \hat{q}(s, a_2; \mathbf{w}) \\ \vdots \\ \hat{q}(s, a_m; \mathbf{w}) \end{bmatrix}$$

### 8.3.3 DQN的两大创新

DQN的成功不仅在于用了神经网络，还引入了两个关键技术来解决神经网络的训练不稳定性问题：

#### 创新一：经验回放（Experience Replay）

**问题**：神经网络的训练样本需要独立同分布（i.i.d.），但强化学习中连续的经验高度相关（相邻状态非常相似）。

**解决方案**：把经验存储在一个**回放缓冲区（Replay Buffer）**中，训练时随机采样。

```
经验回放缓冲区（Replay Buffer）：
    存储形式：(S_t, A_t, R_{t+1}, S_{t+1}, done)
    存储容量：通常 10^5 ~ 10^6 条经验
    采样方式：随机均匀采样或小批量采样
```

**伪代码**：

```
初始化回放缓冲区 D（容量为 N）

对每一个回合：
    选择初始状态 S_0
    对每一步 t：
        用 ε-贪婪策略选择动作 A_t
        执行 A_t，观察 R_{t+1}, S_{t+1}, done_t
        
        // 存储经验
        将 (S_t, A_t, R_{t+1}, S_{t+1}, done_t) 存入 D
        
        // 从缓冲区采样训练
        从 D 中随机采样小批量经验 (s_j, a_j, r_j, s'_j, done_j)
        
        对每个采样经验计算目标：
            y_j = r_j + γ·max_a' Q(s'_j, a'; w^-)   如果 done_j = False
            y_j = r_j                                  如果 done_j = True
        
        // 最小化损失函数
        L(w) = Σ_j [y_j - Q(s_j, a_j; w)]^2
        用梯度下降更新 w
```

#### 创新二：目标网络（Target Network）

**问题**：如果用一个网络同时产生目标和进行估计，就像"追着自己的尾巴跑"——目标不断变化，训练不稳定。

**解决方案**：用两个网络：
- **主网络（Main Network）** $Q(s, a; \mathbf{w})$：用来选择动作和计算当前Q值
- **目标网络（Target Network）** $Q(s, a; \mathbf{w}^-)$：用来计算TD目标

目标网络的参数 $\mathbf{w}^-$ 定期从主网络复制（通常每几千步），而不是每步都更新。

```
目标网络的更新：
    每隔 C 步：
        w^- ← w    （将主网络参数复制给目标网络）
```

### 8.3.4 DQN完整算法伪代码

```
算法：DQN（Deep Q-Network）
输入：学习率 α，折扣因子 γ，探索率 ε，缓冲区容量 N，目标网络更新频率 C
初始化：主网络参数 w（随机初始化），目标网络 w^- = w
初始化：回放缓冲区 D = ∅

对 episode = 1, 2, 3, ...：
    获取初始观测 S_0
    对 t = 0, 1, 2, ...（直到回合结束）：
        
        // 1. 动作选择（ε-贪婪）
        以概率 ε：
            随机选择动作 A_t
        以概率 1-ε：
            A_t = argmax_a Q(S_t, a; w)
        
        // 2. 执行动作
        执行 A_t，观察奖励 R_{t+1} 和下一观测 S_{t+1}, done
        
        // 3. 存储经验
        将 (S_t, A_t, R_{t+1}, S_{t+1}, done) 存入 D
        
        // 4. 采样训练
        如果 D 中经验足够：
            从 D 中随机采样小批量经验
            
            对每个采样经验 (s_j, a_j, r_j, s'_j, done_j)：
                // 计算TD目标（用目标网络！）
                如果 done_j = True：
                    y_j = r_j
                否则：
                    y_j = r_j + γ·max_a' Q(s'_j, a'; w^-)
            
            // 计算损失并更新主网络
            L(w) = (1/|batch|) Σ_j [y_j - Q(s_j, a_j; w)]^2
            对 w 做梯度下降：w ← w - α·∇_w L(w)
        
        // 5. 定期更新目标网络
        如果 t mod C == 0：
            w^- ← w
        
        S_t ← S_{t+1}
        如果 done 为真，结束本回合
```

### 8.3.5 DQN的直观理解

**类比：学生备考**

想象一个学生（DQN）在备考：
- **经验回放**：他不是按顺序做练习题，而是把做错的题收集在一个错题本里，复习时随机抽题练习。这样可以避免"刚学完第一章就只练第一章的题"这种局限。
- **目标网络**：他用一个稳定的参考答案（目标网络）来检查自己的答案，而不是用每天都在变的临时答案。这样评分标准一致，学习效率更高。

---

## 8.4 梯度TD方法的理论基础

### 8.4.1 随机梯度下降（SGD）

当我们用函数近似时，目标是找到最优参数 $\mathbf{w}$ 使得 $\hat{v}(s, \mathbf{w}) \approx v_\pi(s)$。

**均方误差（MSE）**：

$$J(\mathbf{w}) = \sum_{s \in \mathcal{S}} \mu(s) \big[v_\pi(s) - \hat{v}(s, \mathbf{w})\big]^2$$

其中 $\mu(s)$ 是状态 $s$ 的访问频率。

**梯度下降更新**：

$$\mathbf{w}_{k+1} = \mathbf{w}_k - \alpha \nabla_{\mathbf{w}} J(\mathbf{w}_k)$$

### 8.4.2 梯度TD更新的一般形式

$$\mathbf{w} \leftarrow \mathbf{w} + \alpha \big[U_t - \hat{v}(S_t, \mathbf{w})\big] \nabla_{\mathbf{w}} \hat{v}(S_t, \mathbf{w})$$

**各情况的 $U_t$ 选择**：

| 方法 | 目标 $U_t$ | 说明 |
|------|-----------|------|
| MC | $G_t$（完整回报）| 无偏，方差大 |
| TD(0) | $R_{t+1} + \gamma \hat{v}(S_{t+1}, \mathbf{w})$ | 有偏，方差小 |
| n步TD | $G_{t:t+n}$ | 偏差-方差折中 |
| TD(λ) | $G_t^\lambda$ | 带资格迹的多步方法 |

### 8.4.3 收敛性说明

| 算法组合 | 收敛性 | 说明 |
|---------|--------|------|
| 表格法 + 任意 | ✅ 收敛到最优 | 理论保证 |
| 线性近似 + MC | ✅ 收敛到局部最优 | MSE最小化 |
| 线性近似 + TD(0) | ✅ 收敛（半梯度）| 收敛点附近 |
| 线性近似 + Sarsa | ✅ 收敛 | 类似TD(0) |
| 神经网络 + Q-Learning | ❌ 可能发散 | 需要DQN的稳定技巧 |

> 神经网络与自举（bootstrapping）的组合可能导致发散，这也是为什么DQN需要经验回放和目标网络等技术来保证稳定性。

---

## 8.5 本章总结

### 核心要点

1. **函数近似的动机**：状态空间太大时，表格法无法存储所有值，需要用参数化函数近似。

2. **线性函数近似**：$\hat{v}(s, \mathbf{w}) = \mathbf{w}^\top \boldsymbol{\phi}(s)$，用梯度下降更新参数，简单高效但需要好特征。

3. **DQN**：用深度神经网络近似Q函数，直接从原始输入学习。
   - **经验回放**：打破样本相关性，提高数据效率。
   - **目标网络**：稳定训练目标，防止震荡发散。

4. **梯度TD方法**：用SGD更新参数，目标 $U_t$ 可以是MC目标、TD目标或n步目标。

### 方法演进图谱

```
表格法
  │
  ├── 状态太多 → 函数近似
  │
  ├── 线性近似（手工特征）
  │     └── 半梯度TD(0) / 半梯度Sarsa
  │
  └── 非线性近似（神经网络）
        └── DQN
              ├── 经验回放（Experience Replay）
              └── 目标网络（Target Network）
```

### 一句话总结

> **函数近似 = 不再死记硬背每个状态的值，而是学一个"估值公式"；DQN = 用神经网络学这个公式，配上经验回放和目标网络来稳定训练。**

---

*本章翻译和解释参考了Sutton & Barto《Reinforcement Learning: An Introduction》第2版第9-10章以及Mnih et al. (2015) DQN论文。*
