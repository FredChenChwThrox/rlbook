# 第8章 值函数近似（Value Function Approximation）

## 8.0 核心问题：表格法的局限性

### 8.0.1 为什么需要函数近似？

如果状态太多，甚至是连续的，就无法为每个状态单独存一个表格值。

### 8.0.2 核心思想

用一个带参数的函数 $V(s;\theta)$ 或 $Q(s,a;\theta)$ 代替查表。

这样做的意义是：

- 节省存储；
- 具备泛化能力；
- 能处理高维输入。

---

## 8.1 从表格到函数：表示方法的演进

### 8.1.1 统一视角

表格法可以看成函数近似的一种极端情况：每个状态一个独立参数。

### 8.1.2 特征向量（Feature Vector）

常见写法是把状态映射为特征向量 $\phi(s)$，再让值函数依赖这些特征。

学习提示：

- 状态是原始输入；
- 特征是我们用来表示状态的方式。

---

## 8.2 线性函数近似（Linear Function Approximation）

### 8.2.1 基本形式

线性近似常写成：

$$\hat{v}(s;\mathbf{w}) = \mathbf{w}^\top \phi(s)$$

白话解释：

- $\phi(s)$ 是状态特征；
- $\mathbf{w}$ 是待学习参数；
- 值函数就是“参数和特征的加权和”。

### 8.2.2 用梯度下降更新参数

如果要让估计更接近目标 $y_t$，常见损失为：

$$\frac{1}{2}(y_t-\hat{v}(s_t;\mathbf{w}))^2$$

对应更新可写成：

$$\mathbf{w} \leftarrow \mathbf{w} + \alpha \big(y_t-\hat{v}(s_t;\mathbf{w})\big)\nabla_{\mathbf{w}} \hat{v}(s_t;\mathbf{w})$$

白话解释：

- 误差大，就沿着减少误差的方向多走一点；
- 误差小，就只做小修正。

### 8.2.3 半梯度TD(0)算法

若把 TD 目标作为监督信号：

$$y_t = R_{t+1} + \gamma \hat{v}(S_{t+1};\mathbf{w})$$

就得到半梯度 TD 思想。

之所以叫“半梯度”，是因为通常只对当前估计项求梯度，不对目标中的下一状态估计反向传播。

---

## 8.3 深度Q网络（Deep Q-Network, DQN）

### 8.3.1 为什么需要神经网络？

当状态是图像、传感器流或高维连续向量时，线性函数往往不够表达复杂关系。

### 8.3.2 DQN的核心思想

用神经网络近似动作值函数：

$$Q(s,a;\theta)$$

再沿用 Q-Learning 的目标：

$$y = r + \gamma \max_{a'} Q(s',a';\theta^-)$$

白话解释：

- 当前网络负责预测当前状态动作值；
- 目标网络提供更稳定的下一状态目标值。

### 8.3.3 DQN的两大创新

1. 经验回放（Experience Replay）
2. 目标网络（Target Network）

经验回放的作用：

- 打破样本的时间相关性；
- 提高样本利用率。

目标网络的作用：

- 避免目标值随着主网络一起剧烈变化；
- 提高训练稳定性。

### 8.3.4 DQN完整算法伪代码

```text
初始化主网络 Q(·;θ) 和目标网络 Q(·;θ-)
初始化经验回放池
重复：
    与环境交互，收集转移样本
    从回放池随机采样小批量
    构造目标 y = r + γ max_a' Q(s',a';θ-)
    最小化 (y - Q(s,a;θ))^2
    周期性同步 θ- ← θ
```

### 8.3.5 DQN的直观理解

它可以看成“Q-Learning + 神经网络 + 稳定训练技巧”。

---

## 8.4 梯度TD方法的理论基础

### 8.4.1 随机梯度下降（SGD）

SGD 是深度 RL 中最常用的优化工具之一。

### 8.4.2 梯度TD更新的一般形式

核心仍是：

- 定义目标；
- 定义误差；
- 沿梯度方向更新参数。

### 8.4.3 收敛性说明

一旦进入函数近似，尤其是非线性近似，理论分析会显著比表格法复杂。

学习提示：

- 入门时先抓住“为什么需要近似”和“训练时在优化什么”。
- 严格收敛证明可以后续再补。

---

## 8.5 本章总结

### 核心要点

- 表格法不适合大规模或连续状态空间。
- 函数近似让值函数具备泛化能力。
- DQN 是把 Q-Learning 推到深度学习场景的代表方法。

### 方法演进图谱

表格值函数 → 线性近似 → 非线性近似 → 深度神经网络

### 一句话总结

> 值函数近似的目标，是用有限参数去表示本来无法枚举的巨大状态空间。
