# 第1章：基本概念（Basic Concepts）

## 学习目标

- 理解状态、动作、奖励、策略、回报这些基本对象。
- 知道确定性与随机性在强化学习里分别体现在哪里。
- 理解折扣回报和 MDP 的定义。

---

## 1.1 网格世界示例（Grid World Example）

网格世界是一个常见入门环境，因为它把强化学习问题压缩成了最容易观察的形式：

- 机器人所在的位置就是状态。
- 上下左右或停留就是动作。
- 到达目标或撞到障碍会带来不同奖励。

本节最重要的不是地图本身，而是学会把一个任务拆成“状态 + 动作 + 奖励 + 转移”。

---

## 1.2 状态（State）与动作（Action）

### 状态（State）

- 状态描述了智能体在某一时刻做决策所需的信息。
- 通常记为 $s$，状态集合记为 $\mathcal{S}$。

直观理解：

> 状态就是“你现在所处的情境”。如果这个情境信息不完整，后面的决策就可能做错。

### 动作（Action）

- 动作是智能体在状态下可以采取的选择。
- 通常记为 $a$，在状态 $s$ 下可选动作集合记为 $\mathcal{A}(s)$。

在网格世界里，动作通常是：

- 向上
- 向下
- 向左
- 向右
- 原地不动

学习提示：

- 状态回答“现在在哪”。
- 动作回答“接下来做什么”。

---

## 1.3 状态转移（State Transition）

状态转移描述执行动作后环境如何变化。

### 确定性转移（Deterministic Transition）

如果在状态 $s$ 执行动作 $a$ 后，下一个状态一定是 $s'$，就叫确定性转移。

### 随机性转移（Stochastic Transition）

更一般的情形是：

$$p(s'|s,a) = P(S_{t+1}=s' \mid S_t=s, A_t=a)$$

白话解释：

- 这个式子表示：现在在 $s$，做动作 $a$，下一步到达 $s'$ 的概率是多少。
- 它刻画的是环境的不确定性。

学习提示：

- 策略的随机性来自“你怎么选动作”。
- 转移的随机性来自“环境会怎样响应你的动作”。

---

## 1.4 策略（Policy）

策略规定了智能体在每个状态下如何选择动作。

### 确定性策略（Deterministic Policy）

确定性策略可以写成：

$$a = \pi(s)$$

意思是：在状态 $s$ 下，策略直接给出唯一动作。

### 随机性策略（Stochastic Policy）

随机策略更常写成：

$$\pi(a|s) = P(A_t=a \mid S_t=s)$$

白话解释：

- 这个公式表示：在状态 $s$ 下，选择动作 $a$ 的概率。
- 它不是“环境概率”，而是“智能体自己的选择概率”。

### 策略的表格表示法（Tabular Representation）

在小问题里，可以把每个状态下各动作概率写成表格。

但当状态空间很大时，表格法就不再适用，这也是后面函数近似方法出现的原因。

---

## 1.5 奖励（Reward）

### 奖励函数

奖励描述某一步动作带来的即时反馈。

- 到达目标可能得到正奖励。
- 撞墙、进禁区可能得到负奖励。

### 奖励 vs 回报：一个关键区分

- 奖励 $R_{t+1}$：当前这一步立刻得到的反馈。
- 回报 $G_t$：从当前时刻往后，所有未来奖励的加权总和。

这是一个非常重要的区别，因为强化学习要优化的通常不是“当前这一步拿多少”，而是“长期总收益有多大”。

### 随机奖励

有些环境中，奖励本身也可能是随机的。

学习提示：

- 奖励是局部反馈。
- 回报是长期结果。

---

## 1.6 轨迹、回报与回合（Trajectories, Returns, and Episodes）

### 轨迹（Trajectory）

轨迹是交互过程形成的序列，例如：

$$S_0, A_0, R_1, S_1, A_1, R_2, \dots$$

它记录了“经历了哪些状态、做了哪些动作、得到了哪些奖励”。

### 回报（Return）

折扣回报通常写成：

$$G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1}$$

白话解释：

- $R_{t+k+1}$ 是未来第 $k+1$ 步得到的奖励。
- $\gamma^k$ 用来给更远的未来打折。
- 所以回报本质上是在算“从现在开始，未来所有奖励的折扣总和”。

### 折扣因子（Discount Factor）$\gamma$

- $\gamma$ 的取值通常在 $[0,1]$。
- $\gamma$ 越大，说明越重视长期收益。
- $\gamma$ 越小，说明越偏向短期收益。

### 回合（Episode）

- 有些任务会自然结束，比如走到终点。
- 这种完整过程叫一个回合。

---

## 1.7 马尔可夫决策过程（Markov Decision Processes, MDP）

### 马尔可夫性（Markov Property）

马尔可夫性强调：

> 只要当前状态信息完整，未来就只依赖当前状态，而不必依赖更久远的历史。

### MDP 的正式定义

一个 MDP 通常写成：

$$\langle \mathcal{S}, \mathcal{A}, p, r, \gamma \rangle$$

各部分含义：

- $\mathcal{S}$：状态集合
- $\mathcal{A}$：动作集合
- $p$：状态转移概率
- $r$：奖励机制
- $\gamma$：折扣因子

白话解释：

> MDP 就是把“决策问题”写成一个数学模型。后面几乎所有 RL 公式都建立在这个模型上。

---

## 1.8 总结

### 核心概念回顾

- 状态：做决策时所需的当前信息。
- 动作：在当前状态下的可选行为。
- 策略：状态到动作的选择规则。
- 奖励：一步的即时反馈。
- 回报：未来奖励的折扣总和。
- MDP：把整个问题组织起来的标准框架。

### 关键公式速查

随机策略：

$$\pi(a|s) = P(A_t=a \mid S_t=s)$$

状态转移概率：

$$p(s'|s,a) = P(S_{t+1}=s' \mid S_t=s, A_t=a)$$

折扣回报：

$$G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1}$$

### 从本章到下一章的过渡

第1章定义了问题对象，第2章将进一步回答：

> 给定一个策略后，怎样判断它到底好不好？

## 本章总结

本章最关键的是建立统一语言。后面所有算法虽然形式不同，但都离不开：

- 在什么状态下；
- 采取了什么动作；
- 得到了什么奖励；
- 将来还能得到多少回报。

## 关键概念回顾

- “当前一步”看奖励。
- “长期效果”看回报。
- “整类问题”用 MDP 描述。
